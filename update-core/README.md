# 更新核心（update-core）

独立 TS 更新模块的源码家：以后独立成包时整个目录拎走（538 落点决议）。

## 里面有什么

- `src/ports.ts`：插口定义（只放类型，唯一的运行时内容是一行模块标识）。
  核心只管做决定，插口由核心定，跑腿的活全在外面。
- `src/service.ts`：业务流程。只记得规矩（查状态读本地、查新版联网问一次、
  凭证加指纹、装前复核、请求编号去重、加锁串行、备份顺序、后台执行、重启确认、
  残留自愈），不动手装。零导入，依赖全当参数传。
- `src/commands.ts`：安装命令的纯拼接，两条产物：执行配方（按宿主种类选路由，
  一律精确版本加官方源加 `--save-exact`）与给人复制的手工兜底命令。
  自包含零引用（同层互引门禁要求），类型引用只用 import type。
- `src/runtime.d.ts`：核心允许触碰的通用运行时形状（TextEncoder、URL）。
  只有 Node 宿主与浏览器闭包两边都有的能力才允许出现在这里。
- `build.mjs`：编译脚本。esbuild 只转译不打包，一对一生成
  `src/shared/update/` 下的同名 JS；另跑 `tsc --noEmit` 只做类型检查。
- `tsconfig.json`：类型检查配置（严格模式，不产出）。

## 给第二家的三条保证（538 决议）

1. 输出目标定死保守版本（ES2020），两边都跑得动。
2. 核心里不碰任何 Node 或浏览器专属能力，跑腿全在适配器。
3. 类型只用标准写法，不依赖特殊编译选项。

第二家用自己的工具链直接吃 TS 源码，我们的编译只服务 deck。

## 常用命令

- `node update-core/build.mjs`：类型检查加转译，生成物提交到 git。
- `node tests/verify-update-freshness.js`：新鲜度门禁
  （TS 改了而 JS 没重新生成就变红，另含只读行为契约自检）。

## deck 侧落点

- 纯逻辑进 `src/shared/update/`（文件之间不互相引用，同层互引门禁天然能过）。
- 宿主薄胶水进 `src/host/update.js`：三个电话加宿主接线（读取器在 `src/host/updateReader.js`，
  落盘与安装执行在 `src/host/updateStore.js`，同层边记基线）。
- 安装执行端口（#548 落地，口径见 `docs/adr/20260908-update-install-execution-port.md`）：
  核心按宿主种类出配方，适配器只跑两条路由——桌面宿主走公开服务 `desktopPnpm.runPlugin`
  （激活使用范围必须就是本插件所在的那个，对不上宁可不装），普通 DSH 宿主走
  「当前运行时的可执行文件 + CLI 的 JS 入口 + 参数数组」经宿主注入的子进程能力起进程。
  三系统同一套参数数组，不按操作系统分支，不拼 shell、不用 PATH 上的 `dsh` 命令名。
- 面板在设置页标题行：三态按钮、对话框两行字、开始更新后每秒轮询到待重启，
  装不了给原因加手工命令（手工命令同样带官方源与 `--save-exact`，附「网络受限可去掉源参数」的说明）。
- 成品包 `files` 白名单不动（共享层整树进包、宿主整树复制）。

## 日志点（#542 补两电话，#548 补一条常驻事件）

- 新增电话 `wf.updateInstall` 与轮询的查状态调用均复用常驻事件（调用与调用失败）。
- #548 新增常驻事件 `update.install.exec`：安装执行的跨进程边界调用与结果
  （路由、是否成功、退出码、耗时；不记命令、路径与使用范围名），附录对照表与计数门禁同步更新。
