# 拉取请求离线桩验收用例清单（#507，地图 #503 收尾）

> 验收对象：#508 契约三字段、#504 房内归一与降级、#505 快照组装、#506 界面四态。
> 做法：全部用内存桩代替真实网络，不调用真实 gh，不出网。
> 门禁：node --no-warnings tests/verify-507-pr-offline.js（27 项全绿）加 npm run verify 全链与覆盖率门禁。
> 日志：只用已有事件名与白名单字段（research/489-appendix.md 第 1 章），不新增事件，附录不动。
> 脱敏：只记原因枚举、通道名、计数与短指纹；令牌原文、登录态原文、路径原文、仓库地址原文、标题原文一律不记。

## A 契约三字段（纯形状，无日志）

| 编号 | 用例 | 离线桩做法 | 断言 | 日志链指纹 | 脱敏 |
| --- | --- | --- | --- | --- | --- |
| A1 | 形状声明齐全 | 读形状文件文本 | 三个字段名都在 | 无（纯形状） | 不涉及原文 |
| A2 | 过滤字段可登記 | 读契约文件文本 | 过滤字段在且注释写明界面过滤分界 | 无（纯形状） | 不涉及原文 |
| A3 | 降级分类有名字 | 读分类文件文本 | 分类函数按名导出 | 无（纯形状） | 不涉及原文 |
| A4 | 池内身份有名字 | 读派生文件文本 | 身份函数与候选函数按名导出 | 无（纯形状） | 不涉及原文 |
| A5 | 空来源给空值 | 空对象调归一 | 假、空、空数组逐票必带 | 无（纯形状） | 不涉及原文 |
| A6 | 坏值收空 | 坏评审与数字时间调归一 | 坏评审丢弃、数字时间收空 | 无（纯形状） | 不涉及原文 |

## B 双路降级合成（桩造失败）

| 编号 | 用例 | 离线桩做法 | 断言 | 日志链指纹 | 脱敏 |
| --- | --- | --- | --- | --- | --- |
| B1 | 四种失败各走各的原因 | 桩让查询失败四次（通用、超时、限流、企业版主机） | 原因分别为通用、超时、限流、企业版主机，且回退后仍有数据 | graphql.fallback（范围加原因）加 issues.fallback（从查询到休息） | 只记枚举，不记令牌与地址原文 |
| B2 | 合成链带耗时 | 同上 | 合成链记进入、出去与耗时毫秒 | fallback.chain（进入、出去、耗时毫秒） | 只记通道名，不记响应原文 |
| B3 | 指纹脱敏 | 同上 | 日志字段转文本后无令牌形状与路径形状 | 同 B1 加 B2 | 命中正则只记规则名 |
| B4 | 过滤字段真消费 | 桩回一工单一拉取请求，加只取拉取请求过滤 | 只剩拉取请求 | 无新增（走主路成功分支） | 不涉及原文 |

## C 快照三态（真、假、省略分键，坏值隔离）

| 编号 | 用例 | 离线桩做法 | 断言 | 日志链指纹 | 脱敏 |
| --- | --- | --- | --- | --- | --- |
| C1 | 同池同挂一图 | 内存后端给一图两票 | 两票同挂 | snapshot.cache.miss（原因空） | 只记原因枚举 |
| C2 | 同号异类不互吞且坏值隔离 | 同号给工单与拉取请求各一张，另给省略与坏值 | 孤儿拉取请求、省略、坏值各在 | 同上 | 只记原因枚举 |
| C3 | 同键双票各记一票 | 同上 | 派生表两票都在且总数对 | 同上 | 只记计数 |
| C4 | 未命中指纹 | 首建时采日志 | 常驻级别且字段只有原因 | snapshot.cache.miss（原因） | 只记枚举 |
| C5 | 只改合并时间换版号 | 改合并时间强制重建 | 版号变化 | snapshot.cache.miss（原因强制） | 不记版本号原文 |

## D 界面四验收（只读源码，不断言样式）

| 编号 | 用例 | 离线桩做法 | 断言 | 日志链指纹 | 脱敏 |
| --- | --- | --- | --- | --- | --- |
| D1 | 页签回归 | 读两容器文本 | 都读能力位且无能力回列表，悬浮容器有注记 | 复用快照链路，无新增 | 不涉及原文 |
| D2 | 评论隐藏双路 | 读详情文本 | 数组与图形状都兼容，任一来源标拉取请求即隐藏 | 复用快照链路，无新增 | 不涉及原文 |
| D3 | 接线可抄 | 读页签文本 | 列表以过滤函数为依据并留后端直调示例 | 复用快照链路，无新增 | 示例用占位地址 |
| D4 | 注记可懂 | 读页签文本 | 写清快照全留过滤归前端 | 复用快照链路，无新增 | 不涉及原文 |

## E 同号异类留痕（已知局限不静默）

| 编号 | 用例 | 离线桩做法 | 断言 | 日志链指纹 | 脱敏 |
| --- | --- | --- | --- | --- | --- |
| E1 | 同号留痕 | 读页签与详情文本 | 明示同号进同一详情且评审合并展示留后续 | 复用快照链路，无新增 | 不涉及原文 |

## F 评论只读（行为仿真，不起浏览器）

| 编号 | 用例 | 离线桩做法 | 断言 | 日志链指纹 | 脱敏 |
| --- | --- | --- | --- | --- | --- |
| F1 | 拉取请求只看不写 | 按源码谓词求值 | 数组与图形状有评论也不给输入框 | 复用快照链路，无新增 | 不涉及原文 |
| F2 | 非拉取请求不变 | 同上 | 省略隐藏、空数组显示 | 复用快照链路，无新增 | 不涉及原文 |
| F3 | 只读提示条件化 | 读详情文本 | 输入区被替代时才提示 | 复用快照链路，无新增 | 不涉及原文 |

## G 失败空态（入口保留，不白屏）

| 编号 | 用例 | 离线桩做法 | 断言 | 日志链指纹 | 脱敏 |
| --- | --- | --- | --- | --- | --- |
| G1 | 加载态 | 读页签文本 | 转圈加文案 | 复用快照链路，无新增 | 不涉及原文 |
| G2 | 失败态 | 读页签文本 | 失败横幅加重试按钮可重拉 | 复用快照链路，无新增 | 不涉及原文 |
| G3 | 空态 | 读页签文本 | 零拉取请求给空文案 | 复用快照链路，无新增 | 不涉及原文 |
| G4 | 详情失败 | 读详情文本 | 重试加源站链接可跳出 | 复用快照链路，无新增 | 不涉及原文 |

## 已知留尾（本次只验不做）

- 同号的普通工单与拉取请求进同一个详情，评审合并展示留后续。
- 悬浮面板本来就没有详情分支，点行后详情走右侧停靠同一机制。
- 前端谓词已具名，分类与身份函数按名可查。
