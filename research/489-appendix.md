# 附录：脱敏字段清单与刷盘规则（#489，#330 附录）

> 地图 #329 子票 #489 · Part of #329 · 前置于 #335（本附录验收前 #335 不得进入设计）
> 落盘日期 2026-09-06 · 分支 research/489-appendix · 基线 origin/main @905762e（含 #332 术语订正）
> 术语以 CONTEXT.md 日志体系词条为准（2026-09-05 定版，承接 #329/#330/#331）。
> 版本与效力：本仓库所有已落地的方案与本附录冲突，以更新日期者为准；未来任何讨论若改动本附录内容，以未来版本为准；今天的内容是当前生效的基线，不是永久不可变。
> 本文是 #330 双研究报告的附录，给 #335 详细设计直接引用。本文只定字段清单、通道数量与刷盘做法，不做界面文案与导出包格式终选，那是 #333 的范围。
> 正源：#331 修正评论为唯一的计数正源（原表 26 去 #45，加上 #9、#35、#38、#41，纠偏后常驻 29 条、按需 16 条，总数 45 不变）。底表 45 条见本仓库 `research/331-critical-paths.md`（归档前在分支 research/331-critical-paths，2026-09-16 并进主线，可用 git show 只读）。
> 修订 2026-09-06（#494 落定，分支 feat/494-retire，基线 origin/main@771bc04）：#22 issuePath.push 与 #34 issuePath.record 退役（原因见 1.3 落定说明），计数改为常驻 27 条、按需 16 条、总数 43 条；只改本文档，不碰实现代码。
> 修订 2026-09-07（#498 覆盖审计，分支 audit/498-log-coverage）：新增事件 6 个（#51 host.start、#52 chain.cache.miss、#53 workspaceStore.miss、#54 client.snapshot.hit、#55 client.snapshot.miss、#56 detail.cache.hit；46～50 归 #499 自监控，本票避让），退役电话 2 个（wf.ping、wf.claim，见 1.7），选择事件加来源字段，计数改为常驻 29 条、按需 20 条、总数 49 条。
> 修订 2026-09-07（#499 自监控，分支 feat/499-selfmon）：日志管道自己盯自己，新增自监控 5 条（编号 46～50，见 1.6 新节，错误与告警级、始终落盘），计数改为常驻 27 条、按需 16 条、自监控 5 条、总数 48 条；对照表与门禁同步更新。
> 合流（#498 与 #499，rebase 到 main@84ebd40）：现常驻 29 条、按需 20 条、自监控 5 条、总数 54 条；自监控节为 1.6，#498 退役电话节为 1.7。
> 修订 2026-09-08（#548 安装执行端口落地）：新增常驻 1 条（#57 update.install.exec，安装执行器跨进程边界调用与结果），计数改为常驻 30 条、按需 20 条、自监控 5 条、总数 55 条；对照表与门禁同步更新。
> 修订 2026-09-10（#583 抽离票执行规格 #591 第 8 条）：三旧事件各加必填 pluginId（成功事件 5 键、失败事件 4 键、执行事件 5 键，加完标识后的键集合为新基线）；无新增事件名，计数不变（常驻仍 30 条）；旧实现暂不带标识（门禁只许少记，仍绿），迁移票 #586 切引用后补齐。
> 修订 2026-09-12（#606 补测量盲区）：新增按需 2 条（#58 exec.run、#59 panel.render，见 1.3 增补说明），计数改为常驻 30 条、按需 22 条、自监控 5 条、总数 57 条；对照表与门禁同步更新。
> 修订 2026-09-13（#618 本地配色文件落地）：新增常驻 1 条（#60 labelColors.write 写入配色文件）、按需 1 条（#61 labelColors.read 读取配色文件，见 1.3 增补说明），计数改为常驻 31 条、按需 23 条、自监控 5 条、总数 59 条；对照表与门禁同步更新。
> 补记 2026-09-13（#620 GitHub 批量改色落地）：**没有新增事件，只新增落点**。GitHub 房间新加的标签配色写路径（`src/host/tracker/backends/github/label-colors-ops.js`：列标签、每条改色、撞到 404 时问一次仓库权限）每调一次 gh 落一行常驻 #5 gh.exec（落点补充见 1.4）；两条宿主电话 wf.listLabels 与 wf.setLabelColors 沿用既有常驻 #26 host.call 与 #27 host.call.fail（见 1.3 的 #627 补记）。计数不变，现值是**常驻 31 条、按需 23 条、自监控 5 条、总数 59 条**。
> 修订 2026-09-14（#635 保存成功后的面板显示落地）：新增按需 1 条（#62 labelColors.panelPatch：保存成功后把后端确认的颜色写进面板那份快照的那一笔记录，三种归宿各一条——当场写动、装进来的快照比记录旧时按记录补色、装进来的快照比记录新时记录作废，见 1.3 增补说明），计数改为常驻 31 条、按需 24 条、自监控 5 条、总数 60 条；对照表与门禁同步更新。
> 修订 2026-09-18（#652 宿主侧工作区根落地）：新增按需 1 条（#63 workspaceRoot.resolve：插件判定「哪个目录算这个会话的工作区」这一步的结果与它那层 30 秒缓存的命中情况，见 1.3 增补说明），计数改为常驻 31 条、按需 25 条、自监控 5 条、总数 61 条；对照表与门禁同步更新。
> 修订 2026-09-19（#655 初始化时由用户选域文档布局）：新增按需 1 条（#64 inject.decision：初始化那段文案「这次该不该注入、按哪种布局注入」这个决定的结果，见 1.3 增补说明），计数改为常驻 31 条、按需 26 条、自监控 5 条、总数 62 条；对照表与门禁同步更新。
> 修订 2026-09-20（#663 状态栏横幅链按新流程走）：新增常驻 1 条（#65 guide.inject：首开引导链那条横幅的按钮点下去，给出去的是哪一类东西，见 1.3 增补说明），计数改为常驻 32 条、按需 26 条、自监控 5 条、总数 63 条；对照表与门禁同步更新。
> 修订 2026-09-21（面板只留 DSH 原生右侧边栏一条路）：**退役常驻 1 条**（#35 settings.save —— 它唯一的落点是设置页那一项「打开位置」的即时保存，那一项随本次改造删除，落点没了事件也就退役了，行保留只作追溯）；另有两个事件的字段各减一项：panel.open 不再记 mode（面板只有一条路之后「走哪个入口」没有第二种取值），panel.render 的「打开面板」这一路也不再记 mode（`mode` 仍留在白名单里，只剩更新面板那条线在用它标 `update`）。计数改为常驻 31 条、按需 26 条、自监控 5 条、总数 62 条；对照表与门禁同步更新。
> 修订 2026-09-21（用户报「选完后端要等一会儿才有下一步」）：新增按需 1 条（#66 chain.stale.drop：链快照那一次请求回来时，如果这一次回包已经不算数了 —— 典型现场是点「确认并继续」之后立刻重取一次链，而选后端之前那次请求还在飞，它不带后端段 —— 晚到的旧结果会被丢弃、不写进会话状态；这一次「不算数」的判据当天稍晚更正过，见 1.3 的更正说明），计数改为常驻 31 条、按需 27 条、自监控 5 条、总数 63 条；对照表与门禁同步更新。
> 修订 2026-09-21（用户报「选 Markdown → 确认切换 → 没有任何反应」）：新增按需 1 条（#67 snapshot.stale.drop：面板那份快照回来时，如果这一次回包已经不算数了 —— 典型现场是切换后端之前飞着一次强制重取，绑定成功之后那次重取被「同工作区」的去重规则当成同一次请求复用掉，面板因此拿回切换前那份快照，看上去就是「点了确认没反应」），计数改为常驻 31 条、按需 28 条、自监控 5 条、总数 64 条；对照表与门禁同步更新。
> 修订 2026-09-22（#685「体检」按钮落地）：新增常驻 1 条（#68 healthCheck.inject：面板 KPI 那一行右边缘那颗「体检」按钮点下去，开一个新会话并把当前后端的体检提示词带过去 —— 照 #662 为横幅注入定的先例，用户点一下、插件往会话里写字这件事留痕），计数改为常驻 32 条、按需 28 条、自监控 5 条、总数 65 条；对照表与门禁同步更新。
> 修订 2026-09-22（#689「数字走后端计数」落地，**条数不变**：常驻 32 条、按需 28 条、自监控 5 条、总数 65 条照旧）：既有事件 #10 `snapshot.built` 扩了三个字段 —— `open`（后端计数说的未关闭工单张数）、`closed`（后端计数说的已关闭工单张数，拿不到计数时两个都记 -1）、`partial`（手上这份行数据全不全）。为什么加在既有事件里而不新增一条：这一条每次重建快照都会走到，新加一条会把常驻名单顶上去；而这后一件事正是「数字对不对、清单全不全」要查的那一处，字段加在同一条上最省也最好查。落点两处：`src/host/sessionSnapshot.js` 与 `src/host/sessionRefresh.js` 的 `adoptSnapLog`（同一个事件的两次调用，写法一致）。字段表已同步到第 1 章那一行。
> 修订 2026-09-22（#683「用户选的后端按工作区存一份在宿主侧」落地）：新增 5 条 —— 常驻 2 条告警级（#69 `choiceStore.file.bad` 那份记忆文件读不出来；#70 `choiceStore.write.fail` 写不进去）、按需 3 条（#71 `choiceStore.read` 读到没有、#72 `choiceStore.hint.reject` 客户端手里那版比宿主记的旧、被顶回、#73 `choiceStore.evict` 超过上限丢了最旧的几条）。为什么把「文件坏」与「写失败」定成常驻告警级而不是按需：这两件事就是「这个工作区为什么每次都重新问一遍」的唯一线索，真出问题时用户不会先打开调试开关（同文件 `storage.fail` 是同一种定性）；另三条是高频路径与罕见但无需常年在场的轨迹，按需即可。计数改为常驻 34 条、按需 31 条、自监控 5 条、总数 70 条；对照表与门禁同步更新。
> 修订 2026-09-22（#690「已关闭票按需翻页」落地）：新增常驻 1 条（#74 `issues.page`：界面向后端要一页历史票时记一行 —— 翻的是哪一档状态、筛了几个标签、这一次取回几行、后端说一共多少行、等了多久、成没成，见 1.3 的增补说明）。为什么定常驻：这是用户自己点出来的取数（展开折叠行、筛到已关闭、滚到底各一次），次数少、跨了客户端到宿主那一道边；取回 0 行、总数对不上、超时这几件事要查的时候，用户不会先打开调试开关。计数改为常驻 35 条、按需 31 条、自监控 5 条、总数 71 条；对照表与门禁同步更新。
> 修订 2026-09-23（#714「会话↔票 处理链（数据侧）」落地）：新增常驻 1 条（#80 `sessionTickets.chain`：这张链自己产生的轨迹 —— 一次写记进了哪个会话的链、这一次落盘成没成、重启后从落盘读回了没有，见 1.3 增补说明）。为什么定常驻：写票是低频动作（一次几行），而「界面上为什么没有这张票 / 这条链是怎么记的」正是这一票要能回答的问题，查它的人不会先去打开调试开关；读那一路（顺手看一眼）一条都不记，所以这一族不会刷屏。编号取 80：#78、#79 已被同一天落地的 #707（T3，视野模型的两条轨迹）占用，本票避让，不占别人的号。计数改为常驻 36 条、按需 34 条、自监控 5 条、总数 75 条；对照表与门禁同步更新。
> 补记 2026-09-24（#713「七个后端无关工具」落地）：**没有新增事件，只新增落点**，三条名单与条数照上一段不动（常驻 36 条、按需 34 条、自监控 5 条、总数 75 条；以本节最后一条修订为准）。AI 通过这七个工具（`deck_context`、`deck_issue_get`、`deck_map_snapshot`、`deck_issue_create`、`deck_map_plan_create`、`deck_map_link`、`deck_issue_patch`）发起的每一次调用，沿用既有的常驻 #26 `host.call` 与 #27 `host.call.fail` 各落一行：电话名写在 `method` 字段（取 `deck.issueCreate` 这样的写法），`kind` 一律记 `deck-tool`，字段清单不变（成功行记 method / latencyMs / ok / kind；失败行记 method / kind / errorHash，错误文本先截断再散列）。落点一处：`src/shared/deck-tools/shell.js` 的 `fire()`（七个工具共用同一个壳，所以七个工具只有这一处落点，不重复七份）。这些工具交给后端干活时走的是既有那一层（GitHub / GitLab 房间起 gh / glab 进程，落的还是既有的 #5 `gh.exec` 那一族），没有新开一条通道。
> 修订 2026-09-25（#709（T5）与并行各票的埋点登记收口，**只补登记、不改行为**）：五条早已在代码里发出去、却没登记的事件补进对照表 —— 常驻 1 条（#83 `chain.backoff`：链的退避态每一次求值的结论 —— 该不该真重算、为什么被挡下、还差多久、退到第几档、哪种事件带上来的），按需 4 条（#84 `chain.cache.write` 退避态这份内存缓存的写回、#85 `chain.event` 链改事件驱动之后的四种来路、#86 `naming.guard.event` 命名守护客户端拉询改事件驱动之后的来路、#87 `chain.preflight.reuse` 一次求值里环境预检真问了几条与复用省掉几次）。另订正 #18 `chain.cache.hit` 的字段清单：它的落点已经变成「被退避挡下、直接回上一份快照」，实现记 `keyHash`、`lang`、`deferred`、`reason`、`waitMs` 五个键，`ageMs` 早已不再发射、从白名单里去掉。计数改为常驻 37 条、按需 42 条、自监控 5 条、总数 84 条；对照表与门禁（`tests/verify-log-fields.js`、`tests/verify-log-guards.js`、`tests/verify-log-coverage.js`、`tests/verify-log-count.js`）同步更新。

> 修订 2026-09-25（#724「环境三修」落地）：新增常驻 1 条（#88 `host.dispatch.empty`：处理函数回话了、回话里却没有可用内容 —— 哪一种形状、哪条电话、服务这一份的版本，见 1.3 的 ⑦）。为什么定常驻：真机上检查链那条电话 107 次全败、客户端只能反推「回话到了、里面没有内容」，宿主侧一个字都没留下；查它的人正是故障之后才来翻日志。业务性的正常回话（带 error 文本的失败回参）不记，所以它不会在正常工作里刷屏。编号取 88（接在 #83 之后）。另给既有的常驻 #27 `host.call.fail` 补一个字段 `errorKind`（枚举 missing-dep / timeout / throw，只有检查链那条电话在兜底 catch 里带）：从前那只 catch 只记一个错误散列，事后分不清「是接线没把依赖给全」还是「跑起来真的失败了」。计数改为常驻 38 条、按需 42 条、自监控 5 条、总数 85 条；对照表与门禁（`tests/verify-log-fields.js`、`tests/verify-log-count.js` 等）同步更新。
> 修订 2026-09-27（#727「子目录会话第一帧」落地）：新增按需 1 条（#89 `snapshot.late.install`：那次面板取数已经等超时了、可它后来还是回到了，这一小块（selection 与仓库引用）补装上了没有，见 1.3 的 ⑧）。为什么定按需：走到这里本身就是少见的一件事（客户端那条 30 秒死线只结束「这一次等待」，不判「这份结果作废」），而真出问题时要查的正是「为什么这一格后来自己好了 / 为什么一直没好」，同一行先判调试开关。字段两键：`keyHash`（请求发出时那条目录的短散列，绝不记路径原文）、`installed`（布尔：这一小块真的装上了才算）。落点一处：`src/client/kernel/probe-select.js` 的 `_installLateSnapshotSelection`。计数改为常驻 38 条、按需 43 条、自监控 5 条、总数 86 条；对照表与门禁同步更新。
> 补记 2026-09-24（#733「视野心跳一天失败 303 次」落地）：**没有新增事件名，没有新增字段，三条名单与条数照上一段不动（常驻 38 条、按需 43 条、自监控 5 条、总数 86 条）**。这次只给既有常驻 #88 `host.dispatch.empty` 加第四种形状 `unknown-endpoint`：分发时端点表里根本没有这条电话（新界面调旧宿主、旧宿主继续服务那段升级窗口），与 #724 那三档“回话了却没有内容”是同一类盲区。落点同在 `src/host/rpcChannel.js` 的 dispatchRpcEndpoint，字段仍是 method（电话名明文，如 `wf.focus`，与 #88 既有口径一致，不散列）/ shape / version 三键，不记入参原文。
> 口径落实 2026-09-24（#731「慢问题只能靠反推」）：**没有新增事件，没有新增字段，三条名单与条数照上一段不动（常驻 38 条、按需 43 条、自监控 5 条、总数 86 条）**。这次只把设计里本来就写明的「常驻信息始终落盘」交出去：放行判断从只看级别改成级别加事件名，常驻名单里的信息开关关着也过，按需与调试照旧只在开关打开时记。点名的三项（`snapshot.request`、`snapshot.built` 含耗时、成功的 `host.call` 含耗时与成功标记）是验收证据，不是全部改动；机制一次修好，以后新增常驻只改名单。字段一个不加不减，高频采样与节流不动，日志体量按天轮转不变。门禁新增 `tests/verify-731-resident.js`（四处名单一致、建成与成功调用行必填耗时、开关关着的行为仿真）。

## 0 结论先行（三件套各一句话）

- 字段清单：86 个事件逐个只记已知安全的字段，令牌与登录态原文永不落盘，工作区原始路径与仓库地址不记原文，标题与错误文本截断后才记，自由文本再用具名正则做第二道掩码，纠偏后常驻 29 条减去已退役 2 条（2026-09-21 再退役 #35，成 3 条）、#498 新增常驻 2 条（#55 客户端快照未命中、#51 宿主启动头）、#548 新增常驻 1 条（#57 安装执行）、#618 新增常驻 1 条（#60 本地配色文件写入）、#663 新增常驻 1 条（#65 首开引导链横幅按钮的注入）、#685 新增常驻 1 条（#68 体检按钮的注入）、#683 新增常驻 2 条（#69 那份记忆的文件读不出来、#70 写不进去）、#714 新增常驻 1 条（#80 会话↔票处理链自己产生的轨迹：记进链、落盘、从落盘读回）、#709 登记收口新增常驻 1 条（#83 链的退避结论：该不该真重算、为什么被挡下、还差多久、退到第几档）、#724 新增常驻 1 条（#88 处理函数回话了、回话里却没有可用内容：哪一种形状、哪条电话、服务这一份的版本），现常驻 38 条、按需 43 条（#618 新增 #61 本地配色文件读取、#635 新增 #62 面板快照补色的那一笔记录、#652 新增 #63 工作区根判定、#655 新增 #64 初始化注入决策、2026-09-21 新增 #66 链快照那次不算数的回包丢弃、#67 面板快照那次不算数的回包丢弃、2026-09-22 #683 新增 #71 读到没有、#72 被更旧的选择顶回、#73 超过上限淘汰、#690 新增 #74 历史票那一页的取数、2026-09-23 #706 第二批新增 #75 一笔花费、#76 一次裁决、#77 一次跳过、#709 登记收口新增 #84 退避态写回、#85 链事件来路、#86 命名守护事件来路、#87 环境预检复用），#499 新增自监控 5 条（编号 46～50，见 1.6 节）；总数 86 条（逐票来龙去脉见 1.3 节，现行数字以 1.3 节最后一条修订为准）。
- 定时器名枚举增补（#498）：命名守护 naming-guardian、命名清扫 naming-sweep 之外，新增命名持久化 naming-persist、客户端命名轮询 naming-poll、状态栏轮询 statusbar-poll、检查页轮询 checks-poll；都只记调度不记每次心跳。
- 退役电话（#498，走 #22/#34 先例）：wf.ping 与 wf.claim 删注册（全仓零调用点、零日志行；探活改走 wf.logGetSwitch，认领取走 wf.handoffResolve），实现留守不断链，冒烟探活改走免参开关读电话。
- 通道批量：客户端每批最多 50 条、每 1000 毫秒发一次、单包上限 128KB 或 100 条先到先截，通道失败时丢弃并计数（不阻塞调用处，不无界缓冲）。
- 刷盘规则：错误与告警入队即触发一次刷盘（崩溃窗口不丢致命日志），同一台电脑开两个宿主进程写同一天文件时以后写为准（不加锁，与快照缓存一致），进程当前目录漂移时启动写入实际目录头、不自动迁移旧日志；#335 落地门禁写成自动判定脚本，跑不过就阻塞发布。

## 1 八十六事件对照表：允许字段、截断或散列规则、命中正则名（另有 3 条已退役，行保留只作追溯；自监控 5 条见 1.6 节；退役电话见 1.7 节）

### 1.1 先说什么叫白名单、截断、散列、正则掩码

- 白名单的意思是只记已知安全的字段，未知字段默认不记。允许字段列就是每个事件能记的全部字段，之外的字段一律不记。
- 截断的意思是文本太长只留前一段并标注已截断。散列的意思是把原始路径或标题换成短的指纹，只用来判断两次是不是同一个，不记原文。
- 正则掩码的意思是对自由文本做第二道网，只覆盖已知高危形状，命中就记命中了哪条规则名，不记原文。白名单是主防线，正则只是兜底。
- 常驻的意思是首轮必补、始终落盘的轻量轨迹（P0，37 条，现行编号见 1.4 节标题；#498 新增 #51 宿主启动头与 #55 客户端快照未命中、#548 新增 #57 安装执行、#618 新增 #60 本地配色文件写入、#663 新增 #65 首开引导链横幅按钮的注入、#709 登记收口新增 #83 链的退避结论，另有 3 条已退役见 1.3 落定说明）。按需的意思是只在调试开关打开时才记的详尽轨迹（P1，42 条，现行编号见 1.5 节标题；#709 登记收口新增 #84～#87），高频的还要采样或节流。自监控的意思是日志管道自己给自己记的故障行（5 条，编号 46～50，见 1.6 节）：管道出故障时自己先记下来，而不是静默，全部取错误与告警级、始终落盘，不设采样。
- 级别沿用 #332 定版：错误与告警始终落盘；信息中常驻 37 条轻量轨迹落盘（按需 43 条见 1.5）；其余信息与调试只在调试开关打开时落盘。

### 1.2 全局脱敏规则（所有事件共用，#330 客户端结论）

- 令牌与登录态原文永不落盘，只记有无与类别。字段名统一用 hasToken 与 kind，不记 token 值、密码值、登录态全文。
- 工作区原始路径不记原文，只记工作区键散列。字段名统一用 cwdHash、keyHash、rawHash，不记绝对路径原文。
- 仓库地址不记原文，只记名称与标识。gh 输出只记类别与退出码与截断后首行，不记全文。
- 标题截断 80 字或只记散列（沿用当前代码把标题取前 80 字的榜样）。错误文本截断 120 字，详情错误截断 160 字，并标注已截断。
- 需要看分布时只记计数与前几个编号，不记全量数组。散列只用来判断同一性。

具名正则清单（第二道网，命中只记规则名不记原文）：

- R_TOKEN_BEARER：形如 bearer 加令牌的形状。
- R_GH_TOKEN：形如 ghp_、gho_、github_pat_ 开头的令牌形状。
- R_PASSWD_KV：形如 password、passwd、pwd 等键名带值的形状。
- R_AUTH_HEADER：形如 Authorization 头的形状。
- R_WIN_ABS：形如盘符加冒号加反斜杠的 Windows 绝对路径形状。
- R_HOME_PATH：形如用户主目录开头的路径形状。
- R_REPO_URL：形如仓库地址的形状（含带令牌的地址）。
- R_EMAIL：形如邮箱的形状。
- —：该事件没有自由文本，白名单已够，不需正则。

截断与散列代号：

- T80：标题留前 80 字并标注已截断。
- T120：错误文本留前 120 字并标注已截断。
- T160：详情错误留前 160 字并标注已截断。
- H_CWD：路径只记散列，不记原文。
- H_TITLE：标题只记散列或截断，不记原文。
- H_ERR：错误原文只记散列，不记原文。
- B_TOKEN：令牌只记有无与类别，不记原文。

### 1.3 纠偏说明（以 #331 修正评论为准）

- 原表常驻 26 行（不是 27），按需 19 行（不是 18），总数 45 不变。
- 分级错位 5 处：常驻里混入调试事件 #45 脱敏守卫自身计数；按需里混入信息事件 #9 拉取回退、#35 设置保存、#38 状态栏退化与告警事件 #41 链步骤派生错误。
- 纠偏动作：#45 移出常驻；#9、#35、#38、#41 纳入常驻。纠偏后常驻 29 条、按需 16 条。
- 纠偏时常驻 29 条编号（历史快照，现行见下落定后清单）：1、3、4、5、6、7、8、9、10、13、14、16、20、21、22、26、27、28、31、32、33、34、35、36、38、39、40、41、42。
- 按需 16 条编号（纠偏与落定后均不变）：2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45。
- 落定说明（#494，2026-09-06，基线 origin/main@771bc04）：#22 issuePath.push 与 #34 issuePath.record 退役，不再埋点。原因是 #345 已彻底移除状态栏定位标志与其全部检测和轮询（胶囊界面、本地存储、宿主运行 gh 名单检测与事件队列、每 4 秒轮询电话、改名信号附带记账），在基线全仓查不到等价埋点位置：宿主侧无事件队列与轮询电话、无任何日志发射这两事件，客户端侧记录函数只剩无人调用的空壳。两事件在表内保留原行只作追溯。
- 落定后现行清单：常驻 27 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、35、36、38、39、40、41、42；按需 16 条编号不变。
- 增补说明（#498，2026-09-07，总指挥放行）：新增事件 6 个，编号 51 起往后排（46～50 归 #499 自监控，见撞车裁定）。常驻新增 #55 客户端快照未命中（信息，内存加磁盘都 miss 才记，低频）、#51 宿主启动头（信息，启动写一次，目录按 3.3 记实际目录）；按需新增 #52 链缓存未命中、#53 内存选择缓存未命中、#54 客户端快照命中（百一采样）、#56 详情缓存命中（百一采样），都只在调试开关打开时记。增补后现行清单：常驻 29 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、35、36、38、39、40、41、42、51、55；按需 20 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56。选择事件 #14 加来源字段 caller（宿主侧调用名，如 wf.selection、snapshot-early、detect，不记客户端原文）。
- 增补说明（#548，2026-09-08）：新增常驻 #57 update.install.exec（信息，安装执行器跨进程边界调用与结果；用户点按钮安装才发生，低频不采样）。增补后现行清单：常驻 30 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、35、36、38、39、40、41、42、51、55、57；按需 20 条编号不变（2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56）。
- 增补说明（#606，2026-09-12）：新增按需 2 条。起因：面板列表的主取数路走宿主给的 `ctx.exec`，而房内那条记录点要求调用上下文自带记录器，各操作上下文都没带，于是这条路一条日志都不留。现在在起进程的两处接缝各补一个 `exec.run`，并把上个会话留在客户端的三处临时测点（把耗时编码进 `panel.open` 的 `mode` 取值、计时值挂全局变量）改造成正式的 `panel.render`。增补后现行清单：常驻 30 条编号不变（1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、35、36、38、39、40、41、42、51、55、57）；按需 22 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59。
- 增补说明（#618，2026-09-13）：新增两条事件——常驻 #60 `labelColors.write`（写工作区里的本地配色文件 `docs/agents/label-colors.json`：首建默认文件与用户保存改色各记一条，用户点一次才发生一次，低频、信息级、始终落盘）与按需 #61 `labelColors.read`（读那份文件：列标签、保存前的读—改—写、票列表上色三条路都会读，高频，只在调试开关打开时记，且在同一行先判开关、关着连字段对象都不组装）。两条都只记工作区键散列（cwdHash），不记路径原文。增补后现行清单：常驻 31 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、35、36、38、39、40、41、42、51、55、57、60；按需 23 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61。计数为常驻 31 条、按需 23 条、自监控 5 条、总数 59 条。
- 增补说明（#627，2026-09-13）：**没有新增事件，只新增落点**，所以常驻、按需、自监控三条名单与条数都不变（当时是常驻 30、按需 22、自监控 5、总数 57；#618 之后是常驻 31、按需 23、自监控 5、总数 59）。新增的是两条宿主电话 `wf.listLabels`（列出标签与颜色）与 `wf.setLabelColors`（批量改色），它们的日志点沿用既有的常驻 #26 host.call 与 #27 host.call.fail —— 一条电话一行，电话名写在 `method` 字段，字段清单不变；落点是 `src/host/workspaceCwd.js`（电话体，两句 `loggedPhone`）。这两条电话交给后端的执行器跑外部命令时，`exec.run`（按需 #58）的 via 取新值 `label-colors`（取值表见 1.5 节开头）。
- 增补说明（#620，2026-09-13）：**没有新增事件，只新增落点**，三条名单与条数都不变（现值为常驻 31 条、按需 23 条、自监控 5 条、总数 59 条）。GitHub 房间实现了「列标签 / 批量改色」两条契约操作（`src/host/tracker/backends/github/label-colors-ops.js`），它每调一次 gh 都落在既有的常驻 #5 gh.exec 上：列标签一次、每条改色一次、撞到 404 时问一次仓库权限（`gh repo view --json viewerPermission`），字段只取 #5 允许的那五个，信息级开关关着时外层先返回、连字段对象都不组装（门禁 `tests/verify-github-label-colors.js` 第六段守着这两条）。
- 增补说明（#635，2026-09-14）：新增按需 1 条 —— #62 `labelColors.panelPatch`。为什么要有它：改色保存成功后，面板不再等那次全量重拉，而是当场把后端确认过的颜色写进面板那份快照，而这笔记录住在面板自己的内存里（`src/client/views/labels/labelColorPatch.js`），它决定「保存成功那一刻面板按什么颜色显示」——这条链路此前没有任何日志点能回答「面板当时到底改没改、有没有被一份更旧的快照盖回去」。三种归宿各记一条，且只有真发生才记：`applied` 保存成功当场写动；`kept` 装进来的快照比那一笔记录旧、按记录补色；`expired` 装进来的快照比记录新、记录作废。只记工作区键散列（cwdHash）与枚举加条数，不记路径原文、不记标签名与色值。增补后现行清单：常驻 31 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、35、36、38、39、40、41、42、51、55、57、60；按需 24 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62。计数为常驻 31 条、按需 24 条、自监控 5 条、总数 60 条。
- 增补说明（#652，2026-09-18）：新增按需 1 条 —— #63 `workspaceRoot.resolve`。为什么要有它：插件从本票起多了一步「判定工作区根」——从会话所选目录逐层向上，找到第一个自带 `.git` 或自带 `docs/agents/issue-tracker.md` 的目录（规则见 `docs/adr/20260918-subworkspace-identity-root.md`），这一步的结果决定后端绑定、面板快照、检查链快照、配色文件落点按哪条目录分桶。它是新加的一层内存缓存（判定结果按所选目录的规整键存 30 秒），按纪律要能回答「命中没有、这次判到哪一层、为什么」。级别定按需：它挂在统一钥匙的必经之路上（每打一条电话都会经过），属高频路径，只在调试开关打开时记，且同一行先判开关；缓存命中按百分之一采样，未命中与过期每次都记（那两种才是要人看的）。字段只有两个散列与两个枚举：所选目录散列、工作区根散列、靠什么标记停下（git / anchor / self / fail / no-fs / no-probe / relative）、这次缓存什么结果（hit / miss / expired / skip），不记路径原文。增补后现行清单：常驻 31 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、35、36、38、39、40、41、42、51、55、57、60；按需 25 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63。计数为常驻 31 条、按需 25 条、自监控 5 条、总数 61 条。
- 增补说明（#655，2026-09-19）：新增按需 1 条 —— #64 `inject.decision`。为什么要有它：初始化这段文案从本票起不再只有一个出口——用户点初始化时，插件先判「这个仓库的域文档布局选过没有」：没选过就弹卡、一个字都不注入，选过了才把带布局结论的全文注入会话；出口还从原来的两条（状态栏黄条、检查页红牌那颗执行初始化按钮）收敛到同一个决策函数。这条决定的结果此前只写在两个裸控制台输出里，既不落盘也不受调试开关管，事后查「为什么点了初始化没反应」没有任何可看的轨迹。级别定按需：它由用户点击触发，一次操作最多一条，但仍是跨出插件边界往会话里写字的动作，按「调用次数少、跨边界的记成常驻」这条纪律本可记常驻；这里记按需，是因为它一次会话里至多几条、且只在排查注入行为时才需要，而常驻名单的门槛是「首轮必补、始终落盘」——真机验收时若发现排查需要它常年在场，再按纪律补进常驻名单。字段只有三个枚举：这次要注入的是哪段文案（prompt，今天恒为 setupRun）、决定了哪一种（kind：setup 注入全文 / repo 缺仓库改发建仓指引 / setup-card 布局未选定先问不注入）、按哪种布局（layout：single 根目录一份 CONTEXT.md / multi 子项目各一份 CONTEXT.md，根目录 CONTEXT-MAP.md / unset 还没选）。不记用户选的仓库、路径、文案原文。增补后现行清单：常驻 31 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、35、36、38、39、40、41、42、51、55、57、60；按需 26 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64。计数为常驻 31 条、按需 26 条、自监控 5 条、总数 62 条。
- 增补说明（#663，2026-09-20）：新增常驻 1 条 —— #65 `guide.inject`。为什么要有它：首开引导链那条横幅的按钮此前注入的是链上这一项的提示句，而那句话在一次改写里被降成了一句陈述句（点一下等于什么都没给），事后查「为什么点了没反应」日志里一笔都没有。这一条记的就是那次点击给出去的是哪一类东西，字段只有两个短枚举：哪一步（步骤 id，与顺序真源 `src/shared/tracker/guide-steps.js` 里的 id 一致，例如 gh:installed）与给出去的是哪一类（text 注入了一段文案 / none 没有文案可注入 / action 交给弹窗那类端点动作）。级别定常驻：维护者 2026-09-19 拍板默认落盘（这条链正是「用错了却查不到」的那一处，不等人先打开调试开关）。落点一处：`src/client/statusbar/bannerChain.js` 的 logGuideInject（状态栏那颗横幅按钮的注入处）。不记注入的正文、不记任何路径。增补后现行清单：常驻 32 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、35、36、38、39、40、41、42、51、55、57、60、65；按需 26 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64。计数为常驻 32 条、按需 26 条、自监控 5 条、总数 63 条。
- 增补说明（2026-09-21，用户报「选完后端要等一会儿才有下一步」）：新增按需 1 条 —— #66 `chain.stale.drop`。为什么要有它：那条「选完后端过一会儿才出现」的根因之一是「点确认之后没有立刻重取链」，补上重取之后出现一个新问题——选后端之前那次链请求（不带后端段）可能还在飞，它晚回来会把刚取到的新快照盖回去，界面就从「还没有远端仓库」退回「什么都没有」。所以链快照回包处加了一道键校验：回来的不是当前那一次就丢弃（对比的是「工作区键 + 后端 + 语言」这一条共享键）。记这一行是为了以后查「横幅又空了一下」时能看出是这一处丢弃造成的。字段只有一个：keyHash（这一次请求的共享键散列，不记路径原文）。级别定按需：它只在「这一次回包不算数」时才走到；落点一处：`src/client/kernel/probe-chain.js` 的 loadChain。增补后现行清单：常驻 32 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、36、38、39、40、41、42、51、55、57、60、65、68；按需 27 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64、66。计数为常驻 31 条、按需 27 条、自监控 5 条、总数 63 条。
- 更正说明（2026-09-21 当天稍晚，**不属于新增事件**，所以三条名单与条数照上一段不动）：#66 `chain.stale.drop` 这一条本身一个字没变（事件名、级别、字段、落点都照上一段），改的是它守着的那道判据怎么判。上一段写的「回来的不是当前那一次就丢弃」，第一版把「当前那一次」在**发请求时**就算成一个固定值 —— 于是它判的其实是「发这一次的时候，上一次还在不在飞」，正好判反：晚到的旧结果被采纳写进会话状态，新的那一次反被丢掉（而且那把键一旦停在被丢掉的那一次上，后面换后端的重取会连着被丢）。现在改成在**回包时**判两问：① 同一个键上后来又发过更新的一次（按这个键上的请求序号比）② 这个会话现在要的还是不是这条链（用同一把键的算法在回包时重算一次再比，与快照那一侧 `probe-snapshot.js` 的 H2 同口径）。判据由 `tests/verify-669-chain-after-pick.js` 的 C、D 两组守着（六种竞态现场 + 四版把它做坏的反证）。
- 增补说明（2026-09-21，用户报「选 Markdown → 确认切换 → 没有任何反应」）：新增按需 1 条 —— #67 `snapshot.stale.drop`。为什么要有它：那条「切换后端之后什么都没有发生」的根因在面板那份快照那一路 —— 切换前已经飞着一次**强制重取**（`wf.refresh`），绑定成功之后那次重取按「同一个工作区」的去重规则被当成同一次请求复用掉了，**根本没有发新请求**，面板于是拿回切换前那份快照（连 selection 都还是切换前的后端），仓库标、引导链看着全没动。两处一起改：① 去重键从「工作区」变成「工作区 + 后端」，换过后端就不是同一次请求了，换后端之后那次重取一定真发一次；② 回包时加一道「这一次还算不算数」的判两问 —— 这个工作区上后来又发过更新的一次（按请求序号比）、或这次问的后端已经不是用户现在选的那个后端，就丢弃、不写进会话状态。记这一行是为了以后查「点了确认没反应」时，一眼能看出是这一处丢弃造成的。字段只有一个：keyHash（这一次请求的工作区键散列，不记路径原文）。级别定按需：它只在「这一次回包不算数」时才走到；判断本身与去重键住在 `src/client/kernel/probe-stale.js`（#669 第 5 件当天从 probe-snapshot.js 拆出：守卫放进去那个文件会到 359 行、超 350 上限；两文件同属一个闭包），落点一处：`src/client/kernel/probe-snapshot.js` 的 loadSnapshot。增补后现行清单：常驻 31 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、36、38、39、40、41、42、51、55、57、60、65；按需 28 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64、66、67。计数为常驻 31 条、按需 28 条、自监控 5 条、总数 64 条。
- 增补说明（2026-09-21，#669 第 6 件，**没有新增事件**，只给已有事件 #64 `inject.decision` 扩了一个取值与一处落点，所以三条名单与条数照上一段不动）：此前这一条只记初始化那段文案（prompt 恒为 setupRun），落点只有 `src/client/kernel/prompts.js` 的 `injectSetupDecision`。这次面板的「切换后端」弹窗按工作区初始化过没有分两条路走：还没初始化 → 经过同一个 `injectSetupDecision`（记的仍是 setupRun，一个字都不用改）；已经初始化 → 注入新模板 `switchAlign`（让 AI 把仓库里记录后端的那几处对齐到新后端），这一处此时一条轨迹都没有，真机出现「切完什么指令都没给」时只能靠猜。于是给它补同一条日志：prompt 记 `switchAlign`、kind 记 `align`、layout 记 `unset`（与布局无关），落点在 `src/client/kernel/store-switch.js`。纪律与原来一致：外层先判调试开关、只记枚举、不记文案原文与路径。

- 增补说明（2026-09-22，#683「用户选的后端按工作区存一份在宿主侧」）：新增 5 条 —— 常驻告警级 2 条、按需 3 条。为什么要有它们：用户选的后端从前只存在浏览器 localStorage 里（按访问地址隔离，换端口/换壳/换机器就失忆），这一票把它按工作区多存一份在宿主侧（`<用户主目录>/.dsh/mattskillsdeck/choices.json`，规则见 `docs/adr/20260921-persist-user-choice-host-side.md`）；两份存储都会旧，所以按宿主发号的修订号判谁说话 —— 这一族事件就是「为什么这次用的是这个后端 / 为什么我选的不算数」的轨迹。逐条：
  ① 常驻 #69 `choiceStore.file.bad`（告警级）：那份文件存在、却读不出来（不是合法 JSON、根不是对象、版本号不认、某栏不是对象、体积超限、读盘失败）。为什么定常驻而不是按需：定稿（ADR 的 R5c）要求「文件坏」留一条 warn，而 warn 始终落盘；真出现半截文件时，这是查「为什么这个工作区又开始每次都问了」的唯一线索，用户不会先打开调试开关。字段只有 `reason` 一个枚举（no-home / too-large / read-fail / not-json / bad-version / bad-workspaces / bad-layouts / not-object）。**同一种原因每个进程只记第一条**：一个长期坏掉的文件会让每一次探测都走到这里，不节流就是刷屏。
  ② 常驻 #70 `choiceStore.write.fail`（告警级）：写那份文件失败（拿不到用户主目录、建目录/写临时文件/改名失败）。为什么定常驻：与客户端那份镜像的写失败事件 `storage.fail` 同一种定性 —— 用户刚选完后端，写不进去必须看得见（界面那边靠绑定回包的 `persisted:false` 说一句，这里留一行可回看的）。字段：`keyHash`（工作区键散列）、`reason`（no-home / write-fail 两个取值）。
  ③ 按需 #71 `choiceStore.read`（调试级）：按工作区读那份记忆，读到一条（hit）还是没有（miss）。这是每次探测都经过的高频路径，同一行先判开关、关着连字段对象都不组装。字段：`keyHash`、`outcome`（hit / miss 两个取值）。
  ④ 按需 #72 `choiceStore.hint.reject`（调试级）：客户端报上来的选择比宿主记的那一版旧（含老客户端没带修订号），于是**不采纳**它、按宿主记的那条回答 —— 这一档就是「localStorage 还是老状态时不许说话」。用户报「刚选完怎么又变回去了」时，这是唯一的轨迹。字段：`keyHash`、`baseRev`（客户端手里那版的修订号）、`rev`（宿主记的修订号）。落点：`src/host/tracker/detection/detectionService.js`。
  ⑤ 按需 #73 `choiceStore.evict`（调试级）：超过条数或体积上限时丢了最旧的几条（ADR 的 R8）。字段：`count`（这一次丢了几条）、`max`（当前条数上限）。落点：`src/host/choiceStore.js` 的两条写路（后端记录与布局）。
  增补后现行清单：常驻 35 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、36、38、39、40、41、42、51、55、57、60、65、68、69、70、74；按需 31 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64、66、67、71、72、73。计数为常驻 35 条、按需 31 条、自监控 5 条、总数 71 条。

- 增补说明（2026-09-22，#690「已关闭票按需翻页」）：新增常驻 1 条（#74）。
  ① 常驻 #74 `issues.page`（信息级）：界面向后端要一页历史票的那一次取数。为什么要有它：已关闭的票在首屏只装最近一百条，用户展开折叠行、筛到「已关闭」、或把列表滚到底时才会按页去取；这条轨迹回答的是「这一页到底取回了几行、后端说一共多少行、等了多久、成没成」—— 取回 0 行、总数与折叠行对不上、超时，全靠它定位。为什么定常驻：用户自己点出来的取数，次数少，且跨了客户端到宿主那一道边（同 `host.call` 那一族）。字段：`state`（这一页要的是哪一档：open / closed，空串表示两档都要）、`labelsCount`（带上去筛的标签个数 —— 只记个数，不记标签名）、`returned`（这一次真取回几行）、`total`（后端说这一档一共多少行；拿不到记 -1）、`latencyMs`、`ok` 是否成功、`errorHash`（失败时错误文本先截断再散列；成功时为空串）。守卫：无外层判断（信息级常驻、用户手势触发、低频），与 `host.call` 同一处置。落点：`src/client/kernel/api-io.js` 的 `fetchIssuesPage`。
  增补后现行清单：常驻 35 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、36、38、39、40、41、42、51、55、57、60、65、68、69、70、74；按需 31 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64、66、67、71、72、73。计数为常驻 35 条、按需 31 条、自监控 5 条、总数 71 条。

- 增补说明（2026-09-22，#691「地图子票拉全、已关闭地图不进首屏」）：**没有新增事件，只新增一条电话与三处落点**，所以三条名单与条数照上一段不动（常驻 35 条、按需 31 条、自监控 5 条、总数 71 条）。新增的宿主电话是 `wf.mapTickets`：用户在面板里打开一张地图时，现去后端把那张地图的子票一次拉全（含已关闭；已关闭的地图不在快照首屏里，所以它们只能这样按需取）。它的日志点沿用既有的常驻 #26 `host.call` 与 #27 `host.call.fail` —— 一条电话一行，电话名写在 `method` 字段（取 `wf.mapTickets`），`kind` 记 `map-tickets`，字段清单不变；落点两处：`src/host/mapTickets.js`（电话体，`phoneLog` 两句）与 `src/client/kernel/api-io.js` 的 `fetchMapTickets`（客户端包装，两句 `log`，与 `wf.issueDetail` / `wf.issuesPage` 同一写法）。这条电话交给 GitHub 房间取子票时，每翻一页落一行既有的常驻 #5 `gh.exec`（`src/host/tracker/backends/github/sub-issues.js`，一页一次）。

- 增补说明（2026-09-23，#706 第二批「唯一出口闸 + 账本」）：新增按需 3 条 —— #75 `quota.spend`、#76 `refresh.decide`、#77 `refresh.skipped`。为什么要有它们：这一批把去 GitHub 的每一笔调用收进一道闸（依据 `docs/architecture/refresh-budget-architecture.html` 第一章 I1 与第三章），闸要做三件事——记账、裁决、推迟，而这三件事此前一条轨迹都没有：花了多少（谁发起、哪一档、哪一桶）、这一笔为什么被放行或被降级、这一笔为什么被跳过（后台档推迟、额度不足、推迟队列过期丢弃、AI 工具被拒）——事后查「额度怎么少了」「为什么没刷新」时，这三条就是唯一的现场。三条都定**按需**（P1）：它们落在每一笔真实出站上，属高频路径，按仓库日志纪律记成「打开调试开关才记」那一档，不占常驻名额；三条都是在同一行先判调试开关、关闭时连字段对象都不组装。字段一律只记枚举、散列与数字：`quota.spend` 记 account（哪一档）、bucket（哪个桶）、requests（几条真实出站请求）、points（几点）、remaining（记完这一笔之后的保守下界剩余）；`refresh.decide` 记 category、kind、verdict（放行 / 降级 / 推迟 / 拒绝）、reason（原因代号，取值表在 policy.js）、tier（当时这一桶的档位）；`refresh.skipped` 记 keyHash（工作区键散列，绝不记路径原文）、kind、reason、pending（推迟队列里现在有几条）。三条的落点都在新目录 `src/host/refresh/` 下：`ledger.js` 的 spend 一处、`gate.js` 的 fireDecide 与 fireSkipped 两处。守卫形态照仓库既有口径写在 1.5 节那三行里；级别按需的依据同 #618 的 `labelColors.read`：挂在必经之路上、每笔都过。增补后现行清单：常驻 35 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、36、38、39、40、41、42、51、55、57、60、65、68、69、70、74；按需 34 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64、66、67、71、72、73、75、76、77。计数为常驻 35 条、按需 34 条、自监控 5 条、总数 74 条。

- 增补说明（2026-09-23，#707 第三批「视野模型 + 心跳 + 客户端节拍」）：新增按需 2 条 —— #78 `attention.report`、#79 `attention.sweep`。为什么要有它们：这一批把「界面正在看哪个工作区」变成一条可靠信号（依据 `docs/architecture/refresh-budget-architecture.html` 第三章「界面层」与第四章第 02 件），并据此决定谁在探测、谁该收摊。这条链路此前没有任何现场：上报是不是真到了宿主、这一笔算不算数、收摊那一刻活跃集合里有谁、定时器起停成了什么样，事后只能靠猜。两条都定**按需**（P1）：上报那条挂在每一次窗口上报上、心跳每 20 秒一次，属高频路径；收摊那条挂在每一次收摊结论上。字段一律只记枚举、散列与数字：`attention.report` 记 kind（六种信号之一：focus 获焦 / panel-open 面板打开 / session-switch 会话切换 / heartbeat 本地心跳 / window-blur 失焦 / panel-hidden 面板藏起来）、windowHash 与 rootHash（窗口标识与工作区根的短散列，绝不记路径原文）、visible 面板可不可见、accepted 这一笔算不算新信号；`attention.sweep` 记 active（活跃集合里几个根）、keep（该有定时器的几个）、cancel（该停掉的几个）、linger 有没有一个收尾节拍、collaped 整桌收没收、keepers「正在看」的根有几个。落点都在 `src/host/refresh/attention.js`（上报那处在 handleFocus 里，收摊那处在 sweep 里）。守卫：两条都先在同一行判调试开关、关闭时不组装字段对象；上报那条按十取一采样（心跳是最密的那一路），收摊那条是节拍级动作、低频不采样。增补后现行清单：常驻 36 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、36、38、39、40、41、42、51、55、57、60、65、68、69、70、74、80；按需 38 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64、66、67、71、72、73、75、76、77、78、79、81、82。计数为常驻 36 条、按需 38 条、自监控 5 条、总数 79 条。

- 增补说明（2026-09-23，#714「会话↔票 处理链（数据侧）」）：新增常驻 1 条 —— #80 `sessionTickets.chain`。为什么要有它：这一票新加了一张链（每个会话在处理哪些票：键含工作区根散列、后端、票键与会话散列；每会话留最近 20 张；内存表加上工作区缓存落盘），同时新加了**一份内存缓存与一份磁盘缓存**（重建、重启都要靠落盘那一份），按仓库日志纪律，缓存要有命中、未命中与坏掉这三种状态可查，链本身也要能回答「这条链是怎么来的」。这一条就是那三件事的轨迹：一次写记进了链（哪一类动作、记完这一格几条）、当场落盘成没成、重启或切进工作区时从落盘读回了几格几条。级别定**常驻**：写票是低频动作（一次几行 JSON），而「界面上为什么没有这张票」正是这一票要能回答的问题，查它的人不会先去打开调试开关；**读那一路一条都不记**（顺手看一眼不进链，见定稿第十二章与第十三章那张表），所以这一族不会刷屏。字段只有两个散列、三个枚举与一个数字：`sidHash`、`rootHash`、`kind`（record 记进链 / flush 显式落盘 / restore 从落盘读回）、`ok`、`action`（动作类别）、`count`（这一格几条）。落点一处两段：`src/host/refresh/sessionTickets.js` 的 note（记进链与当场落盘）与 restore（读回）。纯逻辑（键构造、按会话分格的去重与留存、只记写不记读的过滤判定、落盘形状与隐私自检）在 `refresh-core/src/chain.ts` 的产物 `src/shared/refresh/chain.js`，同一条纪律：产物必须与源码逐字一致。增补后现行清单：常驻 36 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、36、38、39、40、41、42、51、55、57、60、65、68、69、70、74、80；按需 34 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64、66、67、71、72、73、75、76、77。计数为常驻 36 条、按需 34 条、自监控 5 条、总数 75 条。

- 增补说明（2026-09-23，#710 第二批「写事件订阅（工作区白名单 + 三档判定）」）：新增按需 1 条 —— #81 `write.event`。为什么要有它：这一票让插件第一次去订「别人在会话里干了什么」这件事（运行时事件 `tools/result` 为主、会话事件 `session/event` 为辅），而订阅面是整个 DSH：跨会话、跨工作区、跨子代理的事件都会到我们手上。这条轨迹要能回答两个问题：① 一条事件到达后我们判成了哪一档（确定写 / 可疑 / 其余）、凭什么判的（原因代号，取值表在 `src/shared/refresh/write-detect.js` 的 `DETECT_REASONS`）、接下来要做什么（立刻补那一行 / 立刻探一次 / 等兜底节拍）、票号认没认出来；② 是两条订阅路里的哪一条送来的。级别定**按需**：每一次成功的工具调用都会经过它，属高频路径，按仓库日志纪律记成「打开调试开关才记」那一档，不占常驻名额；同一行先判调试开关，关着连字段对象都不组装，另加两条节流 —— 兜底那一档（什么都没触发）根本不记、同一工作区一秒内只记第一条。字段只有一个散列、三个枚举与一个布尔：`keyHash`（工作区键散列，绝不记路径原文）、`shape`（哪条订阅路送来的）、`tier`、`reason`、`action`、`hasTicket`（认没认出票号这个布尔，**票号本身也不记**）。**命令原文一个字都不进日志**：参数只在判定那一步做瞬时匹配（这是这一票的隐私红线），落盘、回界面同样是零。落点一处：`src/host/refresh/writeEvents.js` 的 `logFired`。纯逻辑（三档判定表）在 `refresh-core/src/write-detect.ts` 的产物 `src/shared/refresh/write-detect.js`。增补后现行清单：常驻 36 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、36、38、39、40、41、42、51、55、57、60、65、68、69、70、74、80；按需 35 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64、66、67、71、72、73、75、76、77、81。计数为常驻 36 条、按需 35 条、自监控 5 条、总数 76 条。

- 增补说明（2026-09-23，#708 第三批「行级增量刷新（只补变的那几条）」）：新增按需 1 条 —— #82 `refresh.patch`。为什么要有它：这一票把「每次变化都整池重建」换成「只取变的那几条」（定稿第十一章），同时新加了**一份持久化状态**（每个工作区的水印、世代号与待办，跟着快照缓存那一份文件走，不另开缓存文件），按仓库日志纪律，缓存与跨边界取数都要留下可查的轨迹。这一条回答的是：这一次走的是哪一档（只补那几条薄查询 / 该整池 / 探回来没变 / 被闸推迟 / 过期的结果被丢掉）、为什么、变了几行、真发出去了几条薄查询、水印推没推、待办里现在几条 —— 查「明明改了面板却没动」「补了一半」「为什么白整池一次」时，这是唯一的现场。级别定**按需**：它挂在每一次探测之后（活跃工作区 5 秒一拍那条路），属高频路径，按仓库日志纪律记成「打开调试开关才记」那一档，不占常驻名额；同一行先判调试开关，关着连字段对象都不组装。字段一律只记散列、枚举与数字：`keyHash`（工作区键散列，绝不记路径原文）、`mode`（patch 补行 / rebuild 该整池 / nothing 没变 / deferred 被推迟 / failed 出错 / stale-drop 过期丢弃）、`reason`（原因代号，取值表见 `src/shared/refresh/delta.js`）、`changed`（这一次变了几行）、`queries`（真发出去几条薄查询）、`watermark`（advanced 推了 / held 没推）、`pending`（待办里现在几条）。落点一处：`src/host/refresh/patch.js` 的 fire（补行、该整池、没变、被推迟、出错五条分支都调它）。纯逻辑（差分与合并规则、水印推进的判定、世代号比较、派生值该重算还是标待确认、旧结构判定）在 `refresh-core/src/delta.ts` 的产物 `src/shared/refresh/delta.js`，同一条纪律：产物必须与源码逐字一致。增补后现行清单：常驻 36 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、36、38、39、40、41、42、51、55、57、60、65、68、69、70、74、80；按需 36 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64、66、67、71、72、73、75、76、77、81、82。计数为常驻 36 条、按需 36 条、自监控 5 条、总数 77 条。

- 增补说明（2026-09-25，#709（T5）与并行各票的埋点登记收口）：**只补登记，不改行为**。起因：几张并行的票各自加了新的日志事件与新落点（链的退避态、链与命名守护改事件驱动、环境预检复用），登记没跟上，字段门禁与覆盖门禁因此红着。这次把五条如实补上，另订正一条已有事件的字段清单。逐条：
  ① 常驻 #83 `chain.backoff`（信息级）：链的退避态每一次求值的结论 —— 这一次要不要真重算、被挡下时按规则还差多久、为什么（原因代号）、退到第几档、是哪种事件带上来的。为什么定常驻：#709 把「只要链没全绿就每 8 秒再 force 一次」那条自续循环整条拆掉之后，宿主侧不再有任何自续定时器，下一次求值只由四种事件带起来（切进工作区、点「重新检查」、做完可能改变它的动作、写入成功之后）；「这次为什么没查 / 为什么还在等」事后只能靠这一条回答，而查它的人不会先打开调试开关。字段是六键：`keyHash`（链共享键的短散列，键里含工作区键、后端与语言）、`decision`（这一次的结论：recompute 该重算 / defer 被退避挡下 / user-recheck-bypass 人亲手点的那一次永不降档 / write-reset 写入成功之后把所有链拉回第一档）、`reason`（原因代号，取值来自 refresh-core 的 backoff.ts）、`waitMs`（按规则还差多少毫秒）、`step`（退到第几档）、`trigger`（哪种事件带上来的：user-recheck / enter-workspace / action-done / write-done，兜底记 event）。落点一处三行：`src/host/refresh/chainBackoff.js` 的 verdict（信息级那一行是常驻；人亲手点那一条分支是调试级）与 noteWriteActivity（调试级）—— **允许字段是这几行的并集**，这是本附录「允许字段就是能记的全部字段」那条口径；后两行同一行先判调试开关，关着连字段对象都不组装。
  ② 按需 #84 `chain.cache.write`（调试级）：#709 新加的退避态是一份内存缓存（每个链键退到第几档、这份快照几点求的、全不全绿），写回那一步记一行。为什么定按需：每次求值都会写回一次，属高频路径，记成「打开调试开关才记」那一档；同一行先判开关。字段五键：`keyHash`、`allGreen`（这份快照是不是每一步都 done）、`doneCount`（这一步之前链上已有几步 done）、`step`（写回之后退到第几档）、`progressed`（这一轮算不算有进展 —— 多出一步 done 才算）。落点：`src/host/refresh/chainBackoff.js` 的 note。
  ③ 按需 #85 `chain.event`（调试级）：链的自续轮询退役之后，客户端那几路事件（切进工作区 / 人点「重新检查」/ 做完可能改变它的动作）都走同一个入口（`chainEventRefresh`），这一条记这一次是哪种来路。字段只有一个：`reason`（枚举 enter-workspace / user-recheck / action-done / write-done）。落点：`src/client/kernel/probe-chain.js` 的 chainEventRefresh；同一行先判调试开关。
  ④ 按需 #86 `naming.guard.event`（调试级）：命名守护的客户端拉询同样改成事件驱动（从前那条 5 秒自续轮询整条退役），四种事件到来时催一次那一下记一行。字段只有一个：`reason`（与上一条同一串取值，兜底记 event）。落点：`src/client/kernel/api-naming.js` 的 namingGuardianEvent；同一行先判调试开关。
  ⑤ 按需 #87 `chain.preflight.reuse`（调试级）：一次求值的环境预检收尾 —— 这一轮真问了几条预检命令、复用省掉了几次外部问答、是不是人亲手点的那一次。为什么定按需：每次求值落一行，低频但只有排查「一次求值为什么问了这么多条 REST」时才要看。字段四键：`cwdHash`（工作区短散列）、`checks`（这一轮真问出去几条）、`reused`（同一轮里省掉了几次）、`userAction`（布尔：这一次是不是人亲手点的）。落点：`src/host/detectChain.js` 的 handleChain；同一行先判调试开关（本票把原先写在上一行大括号里的同一判断挪到与落点同一行，判断一个字没减）。
  ⑥ 订正 #18 `chain.cache.hit` 的字段清单（**不是新增事件**）：它的落点已经不是「链缓存命中」，而是「这一次被退避挡下、不真算，直接把上一份快照回给界面」，实现记的是 `keyHash`、`lang`、`deferred`（这一份是退避挡下直接回的）、`reason`（被挡下的原因代号）、`waitMs`（还差多久才该算）；`ageMs` 早已不再发射，按「允许字段就是能记的全部字段」的口径从白名单里去掉。
  ⑦ 常驻 #88 `host.dispatch.empty`（告警级）：**处理函数回话了、但回话里没有可用内容，以及分发时根本不认识这条电话** —— 分发这一层是唯一能看见这两种形状的地方。字段三键：`method`（哪条电话，写法与同族的 `host.dispatch.error` 一致，如 `wf.chain`）、`shape`（形状：`no-value` 处理函数什么都没回 / `null-value` 回的是 null / `no-error` 回的是 `{ok:false}` 却没带 error / `unknown-endpoint` 端点表里没有这条电话，多见于新旧版本错位时旧宿主继续服务，见 #733）、`version`（**服务这一份的版本**：本文件旁边那份 package.json 说的版本号；读不到就如实记 `unknown`，绝不编一个）。为什么定常驻：真机上出过一次「检查链那条电话 107 次全败、回话里没有快照」的故障，当时宿主侧**一个字都没留下**（`rpcChannel` 只对处理函数抛错记账，「回话空」不记），事后只能拿客户端两条散列反推「回话到了、里面没有内容」；#733 那次是同一类盲区的另一半（316 次 `unknown endpoint: focus` 同样一个字没留）；而查它的人正是故障之后才来翻日志，不会先打开调试开关。业务性的正常回话（例如 `{ok:false, error:'…'}` 这种说清了原因的回参）不记。落点：`src/host/rpcChannel.js` 的 dispatchRpcEndpoint（用 `fireLog` 的第二个名字 `log` 发射，原因见该文件里那段说明）。
  ⑧ 按需 #89 `snapshot.late.install`（调试级，#727 新增，见 1.5 节最后一行）：那次面板取数已经等超时了、可它后来还是回到了，这一小块（选择与仓库引用）补装上了没有。字段两键：`keyHash`（请求发出时那条目录的短散列）、`installed`（布尔）。落点：`src/client/kernel/probe-select.js` 的 `_installLateSnapshotSelection`；同一行先判调试开关。
  增补后现行清单：常驻 38 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、36、38、39、40、41、42、51、55、57、60、65、68、69、70、74、80、83、88；按需 43 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64、66、67、71、72、73、75、76、77、78、79、81、82、84、85、86、87、89。计数为常驻 38 条、按需 43 条、自监控 5 条、总数 86 条。

### 1.4 常驻 38 条（P0，始终落盘，低频轻量；#22、#34、#35 已退役，原行保留只作追溯；#51、#55 为 #498 新增，#57 为 #548 新增，#60 为 #618 新增，#65 为 #663 新增，#68 为 #685 新增，#69、#70 为 #683 新增，#74 为 #690 新增，#80 为 2026-09-23 #714 新增，#83 为 2026-09-25 #709 登记收口新增，#88 为 2026-09-25 本票新增）

落点与退役说明（2026-09-21，不属于新增事件）：**#35 `settings.save` 退役**。它的唯一落点是设置页那一项「打开位置」的即时保存；面板改成只有 DSH 原生右侧边栏一条路之后，那一项（连同 cfg.openIn 与七个文案键）整项删除，落点消失，事件随之退役 —— 行留在 1.3 表里只作追溯。同一次改造还让两个事件的字段各减一项：`panel.open`（常驻 #36）不再记 `mode`，「打开面板」那一路的 `panel.render` 也不再记 `mode`（`mode` 仍留在 `panel.render` 的白名单里 —— 更新面板那条线用它标 `update`，见 1.5 节）。现行清单：常驻 32 条，编号为 1、3、4、5、6、7、8、9、10、13、14、16、20、21、26、27、28、31、32、33、36、38、39、40、41、42、51、55、57、60、65、68；按需 26 条，编号为 2、11、12、15、17、18、19、23、24、25、29、30、37、43、44、45、52、53、54、56、58、59、61、62、63、64。计数为常驻 31 条、按需 26 条、自监控 5 条、总数 62 条。

落点补充（#627，不属于新增事件）：下表 #26 与 #27 两行自本票起多两处落点 `src/host/workspaceCwd.js`（标签配色两条电话 wf.listLabels 与 wf.setLabelColors 的记法），字段仍是这两行允许的那几个，`method` 里写电话名。

落点补充（#620，不属于新增事件）：下表 #5 这一行「gh.exec」自本票起多一处落点 —— GitHub 房间新加的标签配色写路径（`src/host/tracker/backends/github/label-colors-ops.js`）每调一次 gh 就落一行（列标签一次、每条改色一次、撞到 404 时问权限一次），字段仍是 #5 允许的那五个（argv0、cwdHash、latencyMs、kind、exitCode），信息级开关关着时外层先返回、不组装字段。本票没有新增事件，常驻仍是 31 条、按需 23 条、自监控 5 条、总数 59 条（#635 之后是常驻 31 条、按需 24 条、自监控 5 条、总数 60 条）。

列说明：允许字段就是能记的全部字段；截断或散列列写每行用的代号；命中正则名列写第二道网用的规则名；原证据列转引 #331 文档的旧基线行号，只作追溯用，本附录新增断言的行号以第 5 节分支基线为准。

| # | 事件名 | 级别 | 允许字段（白名单，之外不记） | 截断或散列 | 命中正则名 | 原证据（#331 基线追溯） |
|---|---|---|---|---|---|---|
| 1 | snapshot.request | 信息 | cwdHash、工作区后端标识、force 是否强制 | H_CWD | R_WIN_ABS、R_HOME_PATH | index.js:1073 快照组装入口、297 找 gh |
| 3 | snapshot.cache.miss | 信息 | reason 未命中原因（枚举 force、empty、version-mismatch、expired） | —（枚举不截断） | — | index.js:47 缓存超时、589 写盘 |
| 4 | repo.resolve.tier | 信息 | tier 第几层、ok 是否成功、latencyMs 耗时 | —（不记地址原文） | R_REPO_URL | index.js:601、605-638 三层兜底 |
| 5 | gh.exec | 信息 | argv0 命令名、cwdHash、latencyMs、kind 归一类别、exitCode 退出码 | H_CWD、B_TOKEN | R_TOKEN_BEARER、R_GH_TOKEN | index.js:311 运行 gh、340 类别归一 |
| 6 | gh.timeout | 告警 | argv0 命令名、timeoutMs 超时毫秒 | — | — | index.js:325 超时、338 |
| 7 | gh.resolve.fail | 告警 | hasDSH_GH_PATH 是否配备用路径、errorHash 错误散列 | H_ERR | R_WIN_ABS | index.js:297、303-305 |
| 8 | graphql.fallback | 告警 | scope 地图或单票、reason 降级原因（枚举） | — | — | index.js:945 配额判断、903、988 |
| 9 | issues.fallback | 信息（纠偏纳入） | from 从哪种拉取、to 回退到哪种、reason 原因 | — | — | index.js:791 对比 822 |
| 10 | snapshot.built | 信息 | maps 地图数、issues 票数、labels 标签数、open 后端计数说的未关闭工单张数（拿不到记 -1）、closed 后端计数说的已关闭工单张数（拿不到记 -1）、partial 手上这份行数据全不全（真 = 被截断过或压根拿不到计数）、fallback 是否降级、latencyMs | —（只记计数） | — | sessionSnapshot.js 与 sessionRefresh.js 的 adoptSnapLog 组装返回（#689 扩了 open / closed / partial 三个字段，条数不变） |
| 13 | panelSync.dirty | 信息 | cwdHash、ageMs 脏了多久 | H_CWD | — | index.js:515-519 脏回执 |
| 14 | registry.select | 信息 | cwdHash、backendId 后端标识、source 选择来源、latencyMs、caller 宿主侧调用名（#498 新增，如 wf.selection、snapshot-early、detect） | H_CWD | — | registry.js、index.js:1146 选择 |
| 16 | detection.detect | 信息 | cwdHash、explicit 是否显式、matches 命中数、pending 是否待定、selection 选中结果 | H_CWD | R_WIN_ABS | detectionService.js:32 空目录判断、index.js:256 技能探测 |
| 20 | skill.probe | 信息 | name 技能名、level 结果等级、via 判装通道（枚举） | 路径只记通道枚举 | R_WIN_ABS、R_HOME_PATH | index.js:50 技能名单、1330-1649 判装 |
| 21 | skill.pending.cap | 告警 | name 技能名、attempts 已试次数、max 上限 3 | — | — | index.js:1338 待定封顶 |
| 22 | issuePath.push（已退役，见 1.3 落定说明） | 信息（已退役，不再埋点） | 原白名单 ref 票号引用、source 来源、queueLen 队列长度（已失效，只作追溯） | H_TITLE（已失效） | R_TOKEN_BEARER（已失效） | index.js:348 白名单、425 入队（已随 #345 移除） |
| 26 | host.call | 信息 | method 调用的电话名、latencyMs、ok 是否成功、kind 归一类别、pluginId 插件标识（#583 起更新电话必填，其余电话暂不带，门禁只许少记） | H_CWD | — | client/kernel/probe.js:40 调链、client/index.js 50 起；更新包宿主入口 packages/dsh-plugin-update/src/host.ts |
| 27 | host.call.fail | 告警 | method 电话名、kind 类别、errorHash 错误散列、pluginId 插件标识（#583 起更新电话必填，其余电话暂不带，门禁只许少记）、errorKind 错误的类别（枚举，只有检查链那条电话在兜底 catch 里带：missing-dep 接线没给全依赖 / timeout 超时 / throw 跑起来真失败；#724 新增，从前只记散列、分不清是装配少了东西还是运行中坏掉） | H_ERR、T120 | R_TOKEN_BEARER、R_GH_TOKEN | 同上；更新包宿主入口 packages/dsh-plugin-update/src/host.ts；检查链兜底 catch 那一处在 src/host/detectChain.js 的 handleChain（#724 补） |
| 28 | snapshot.hydrate | 信息 | cwdHash、source 来源枚举、fresh 是否新鲜、latencyMs、winnerVersion 胜出方版本号、loserVersion 落败方版本号、outcome 合并结果（incoming 缓存胜出、current 原数据保留） | H_CWD | — | kernel/router.js:9 缓存水合 |
| 31 | backend.switch | 信息 | from 从哪个后端、to 到哪个后端、cwdHash | H_CWD | — | views/shared/BackendSelector.js、router.js:39 |
| 32 | naming.guard | 信息 | sidHash 会话散列、outcome 改名结果、hintHash 线索散列 | H_TITLE、T80 | R_TOKEN_BEARER | kernel/api.js:46 改名轮询、shared/naming-guardian.js |
| 33 | naming.lock | 信息 | sidHash、reason 拦截原因（枚举，如用户手改） | — | — | shared/naming-guardian.js 值比对锁 |
| 34 | issuePath.record（已退役，见 1.3 落定说明） | 信息（已退役，不再埋点） | 原白名单 ref 票号引用、source 来源、titleHash 标题散列（已失效，只作追溯） | H_TITLE（已失效） | — | kernel/store.js:81 记录（已随 #345 移除） |
| 35 | settings.save（已退役，见 1.3 落定说明） | 信息（已退役，不再埋点） | 原白名单 openIn 打开位置、tplChangedCount 模板改了几处（已失效，只作追溯） | —（不记模板正文） | — | 原落点为 views/SettingsPage.js 的「打开位置」即时保存；2026-09-21 该项删除后落点消失 |
| 36 | panel.open | 信息 | hasCache 是否有缓存、snapFresh 快照是否新鲜、keyHash 工作区键散列、snapVersion 快照版本号、backendId 后端标识（2026-09-21 去掉 mode：面板只有 DSH 原生右侧边栏一条路，形态没有第二种取值） | H_CWD | — | kernel/router.js 点开面板处 |
| 38 | statusbar.fallback | 信息（纠偏纳入） | reason 退化原因（枚举） | — | — | statusbar/StatusBar.js:59 胶囊永不隐藏 |
| 39 | dock.rehydrate | 告警 | sidHash、cwdChanged 工作区是否变了、polluted 是否污染 | H_CWD | R_WIN_ABS | panel/Dock.js:34 跟随、54 污染自愈 |
| 40 | storage.fail | 告警 | key 键名枚举、op 读或写 | — | — | kernel/store.js:14、kernel/config.js:13 读本地存储 |
| 41 | chain.derive.error | 告警（纠偏纳入） | stepId 步骤标识、errorHash 错误散列 | H_ERR、T120 | R_TOKEN_BEARER | kernel/probe.js:61 链派生 |
| 42 | fallback.chain | 信息 | in 从哪种、out 到哪种、latencyMs | — | — | index.js:903、988、791 |
| 55 | client.snapshot.miss | 信息（#498 新增） | keyHash 工作区键散列、reason 未命中原因（枚举 empty） | H_CWD | — | kernel/probe-snapshot.js 内存加磁盘都未命中才记 |
| 51 | host.start | 信息（#498 新增收录） | pid 进程标识、startedAt 启动时间、dir 实际目录（3.3 启动头所需，记原文） | —（启动头诊断所需，字段固定三键） | — | host/logStore.js 启动头 |
| 60 | labelColors.write | 信息（#618 新增） | cwdHash 工作区键散列、count 这次文件里有多少行标签、ok 这一次写入或放置有没有成功（成功 true、失败 false——文件读不出来被挡下、写失败、选定后端时放置失败，都会记成 false，不是「只记成功」）、reason 原因枚举（placed 首建成功 / saved 保存成功 / refused-read-fail 文件读不出来、拒绝放置也不改写 / write-fail 写失败 / place-fail 用户选定后端时那次放置失败）、via 发布方式枚举（rename 临时文件加改名 / write-text 一次写完整份），只有真写成的那两条成功记录才带 via | H_CWD | — | 本地 Markdown 房间的工作区配色文件写入：首建默认文件与用户保存改色（src/host/tracker/backends/markdown/label-colors.js）；另有一处落点 src/host/workspaceCwd.js 的 handleBind（用户选定后端时那次放置失败记一条） |
| 57 | update.install.exec | 信息（#548 新增） | route 路由枚举（desktop-service 桌面服务、cli-process 自己起进程、none 没拿到配方）、ok 是否成功、exitCode 退出码（拿不到为 null）、durationMs 执行多久、pluginId 插件标识（#583 起必填，旧实现暂不带，门禁只许少记） | —（只记枚举与数字加标识，不记命令、路径、使用范围名） | — | host/updateStore.js 安装执行器（跨进程边界调用与结果，用户点按钮才发生，低频）；更新包执行器 packages/dsh-plugin-update/src/store.ts |
| 65 | guide.inject | 信息（#663 新增） | step 哪一步（首开引导链的步骤 id，与 src/shared/tracker/guide-steps.js 里的 id 一致）、outcome 这次给出去的是哪一类（text 注入了一段文案 / none 没有文案可注入 / action 交给弹窗那类端点动作） | —（只记枚举，不记注入正文） | — | client/statusbar/bannerChain.js 的 logGuideInject：状态栏那颗横幅按钮的注入处（用户点一次落一行；不经过初始化那条漏斗，是独立一条事件） |
| 68 | healthCheck.inject | 信息（#685 新增） | backend 当前是哪个后端、outcome 这一次注入成功还是失败（ok 取到了提示词并交给了新会话 / fail 提示词没取到） | —（只记两个短枚举，不记提示词正文，也不记会话标题） | — | client/kernel/health-check.js 的 openHealthCheck：KPI 那一行右边缘那颗「体检」按钮的点击处（用户点一次落一行） |
| 69 | choiceStore.file.bad | 告警（#683 新增） | reason 读不出来的原因（枚举 no-home 拿不到用户主目录 / too-large 体积超限 / read-fail 读盘失败 / not-json 不是合法 JSON / bad-version 版本号不认 / bad-workspaces 工作区那栏不是对象 / bad-layouts 布局那栏不是对象 / not-object 根不是对象） | —（只记一个枚举） | — | 无外层判断（告警级始终落盘）；**同一种原因每个进程只记第一条** —— 一个长期坏掉的文件会让每一次探测都走到这里，不节流就是刷屏 | src/host/choiceStore.js 的 readFromDisk（读那份记忆失败的那一档） |
| 70 | choiceStore.write.fail | 告警（#683 新增） | keyHash 工作区键散列、reason 写不进去的原因（枚举 no-home 拿不到用户主目录 / write-fail 建目录或写临时文件或改名失败） | H_CWD 同一套短散列 | — | 无外层判断（告警级始终落盘）；用户点确认才可能记，低频 | src/host/choiceStore.js 的两条写路（rememberWorkspace 与 rememberLayout 各一处） |
| 74 | issues.page | 信息（#690 新增） | state 这一页要哪一档（open / closed，空串=两档都要）、labelsCount 带上去筛的标签个数（只记个数不记名字）、returned 这一次真取回几行、total 后端说这一档一共多少行（拿不到记 -1）、latencyMs、ok 成没成、errorHash 失败时错误文本先截断再散列（成功为空串） | H_ERR、T120（失败才散列） | R_TOKEN_BEARER、R_GH_TOKEN（散列前已截断） | 无外层判断（信息级常驻、用户手势触发、低频：展开折叠行 / 筛到已关闭 / 滚到底各一次） | src/client/kernel/api-io.js 的 fetchIssuesPage（客户端要一页历史票的那一次取数，电话为 wf.issuesPage） |
| 80 | sessionTickets.chain | 信息（#714 新增） | sidHash 这个会话的散列、rootHash 工作区根散列、kind 这一次是哪一种（枚举 record 一次写记进了链 / flush 显式落盘一次 / restore 从落盘读回）、ok 这一次成没成（落盘写失败、缓存文件坏、隐私自检没过都会记 false）、action 动作类别（枚举，只有 record 带：create 建票 / plan 批量建票建边 / comment 评论 / edit 改动 / state 关闭重开 / link 建关系 / file-write 直接改票文件 / other-write 其余写）、count 记完这一条或读回之后这一格里现在有几条 | H_CWD 同一套短散列（会话与工作区都只记散列，不记路径原文、不记命令原文、不记票标题） | —（全是枚举、散列与数字） | 无外层判断（信息级常驻；写票是低频动作，一次一行；读那一路一条都不记 —— 顺手看一眼不进链，所以这一族不会刷屏）。这份缓存没有过期一说：留存上限是每会话 20 张，时间只用来排序 | src/host/refresh/sessionTickets.js：note（记进链与当场落盘那一步）与 restore（重启或切进工作区时从落盘读回）两处；纯逻辑与落盘形状在 refresh-core/src/chain.ts 的产物 src/shared/refresh/chain.js |
| 83 | chain.backoff | 信息（#709 登记收口新增） | keyHash 链共享键的短散列（键里含工作区键、后端与语言）、decision 这一次求值的结论（枚举 recompute 该真重算 / defer 被退避挡下、直接回上一份快照 / user-recheck-bypass 人亲手点的那一次永不降档 / write-reset 写入成功之后把所有链拉回第一档）、reason 原因代号（枚举，取值来自 refresh-core/src/backoff.ts）、waitMs 按规则还差多少毫秒、step 退到第几档、trigger 哪一种事件带上来的（枚举 user-recheck / enter-workspace / action-done / write-done，兜底记 event） | H_CWD 同一套短散列（不记路径原文、不记命令行） | —（全是枚举、散列与数字） | 信息级那一行无外层判断（常驻：#709 拆掉 8 秒自续循环之后，这是唯一还能回答「这次为什么没查」的地方）；同族另两行是调试级、同一行先判调试开关，**字段清单是这三行的并集** | src/host/refresh/chainBackoff.js 的 verdict（该不该重算那一处）与 noteWriteActivity（写入成功之后把所有链拉回第一档）；数字的唯一真源是 refresh-core/src/budget.ts |
| 88 | host.dispatch.empty | 告警（#724 新增，#733 加第四种形状） | method 哪条电话（写法与同族的 host.dispatch.error 一致，如 wf.chain）、shape 形状（枚举 no-value 处理函数什么都没回 / null-value 回的是 null / no-error 回的是 ok:false 却没带 error / unknown-endpoint 端点表里没有这条电话，多见于新旧版本错位，见 #733）、version 服务这一份的版本（本文件旁边那份 package.json 的版本号，读不到记 unknown） | —（只记枚举与一个版本号：不记入参原文、不记回话内容、不记路径） | —（全是枚举与一个版本号） | 无外层判断（告警级始终落盘）；只在“回话了却没有可用内容”或“根本不认识这条电话”时走到 —— 业务性的正常回话（例如带 error 文本的失败回参）不记，所以正常工作里不会刷屏；同一个故障形状每次调用各一行，与同族的 host.call.fail 同一量级 | src/host/rpcChannel.js 的 dispatchRpcEndpoint（#724 补：真机上「回话里没有快照」这件事从前一个字都没留下，只能靠客户端散列反推；#733 补另一半：316 次 unknown endpoint 同样没留） |

### 1.5 按需 43 条（P1，只在调试开关打开时记，高频要守卫；#52、#53、#54、#56 为 #498 新增，#58、#59 为 #606 新增，#61 为 #618 新增，#62 为 #635 新增，#63 为 #652 新增，#64 为 #655 新增，#66 为 2026-09-21 新增，#67 为同日新增，#71、#72、#73 为 2026-09-22 #683 新增，#75、#76、#77 为 2026-09-23 #706 第二批新增，#78、#79 为同一天 #707 第三批新增，#81 为同一天 #710 第二批新增，#82 为同一天 #708 第三批新增，#84、#85、#86、#87 为 2026-09-25 #709 登记收口新增）

exec.run 的 via 取值表（枚举写死在这里）：snapshot 面板快照组装（wf.snapshot，两处分支）、refresh 面板刷新组装（wf.refresh，两处分支）、detect-select 探测级联的三选一、detect-preflight 探测级联的后端预检、snapshot-select 快照里补做的一次三选一、viewer-lookup 快照里预取当前登录用户、comment-write 评论写回、publish 初始化仓库与推送（本文件里全部 git 命令）、repo-root 解析仓库根、repo-key 解析仓库键、label-colors 标签配色两条电话交给后端的执行器（#627：列出标签与颜色、批量改色；房间内外都走它）、unspecified 兜底（调用方没把链名传下来时用它；现在源码里没有任何一条命令会走到这个取值）。

| # | 事件名 | 级别 | 允许字段（白名单，之外不记） | 截断或散列 | 命中正则名 | 守卫 | 原证据（#331 基线追溯） |
|---|---|---|---|---|---|---|
| 2 | snapshot.cache.hit | 调试 | kind 取值 snapshot-lru（快照最近最少使用缓存）、ageMs 缓存多久 | — | — | 采样（如百分之一） | index.js:576 读盘、589 写盘、47 超时 |
| 11 | probe.eval | 调试 | repoKeyHash 仓库散列、since 增量起点、count 拉到几条、changed 是否有变化 | 散列仓库键 | — | 采样，只记有变化 | index.js:847 拉索引、871 判变化 |
| 12 | panelSync.eval | 调试 | repoKeyHash、baseline 基线、dirty 是否脏、failures 失败数 | 散列仓库键 | — | 按事件，有变化才记 | index.js:451 求值、494 单个求值 |
| 15 | registry.stub | 调试 | op 操作名、backendId 后端标识 | — | — | 按事件 | registry.js:41 不支持桩 |
| 17 | workspaceStore.hit | 调试 | keyHash 键散列、fresh 是否新鲜、ttlMs 存活毫秒 | H_CWD | — | 采样 | workspaceStore.js:38 是否新鲜、48 存活 30 秒 |
| 18 | chain.cache.hit | 调试 | keyHash 链共享键的短散列、lang 语言、deferred 这一份是「被退避挡下、直接回的上一份快照」（布尔）、reason 被挡下的原因代号（枚举，取值来自 refresh-core/src/backoff.ts）、waitMs 还差多少毫秒才该真算（2026-09-25 订正：这个事件的落点已经变成退避挡下那一处，实现记这五个键；从前的 ageMs 早已不再发射，按「允许字段就是能记的全部字段」的口径去掉） | H_CWD 同一套短散列（不记路径原文） | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象） | src/host/detectChain.js 的 handleChain（退避说这一次不必真算，这一行就是那次「拿上一份快照回界面」的记录） |
| 19 | chain.predicate | 调试 | id 谓词标识枚举、status 通过或失败或待定、latencyMs | 详情不记 | R_TOKEN_BEARER | 节流（15 秒超时只记状态变化） | index.js:1721-1745 谓词注册 15000 毫秒 |
| 23 | workspaceKey.canonical | 调试 | rawHash 原始散列、normalizedHash 归一后散列、fallback 是否走兜底 | 双散列，不记原文 | R_WIN_ABS、R_HOME_PATH | 按事件 | workspaceKey.js:28、index.js:536 统一钥匙 |
| 24 | platform.resolve | 调试 | name 找哪个程序、ok 是否找到、latencyMs | — | — | 按事件 | platform/index.js:30 工厂、index.js:176 找程序 |
| 25 | naming.sweep | 调试 | trigger 触发来源枚举、count 扫到几条 | 线索只记计数 | — | 节流（只记结果变化） | index.js:365 延迟扫、shared/naming-guardian.js |
| 29 | snapshot.fanout | 调试 | sessionIdHash 会话散列、stale 是否旧数据、force 是否强制、count 复用组大小（只在接收方带，发起方不带） | 散列会话 | — | 按事件（发起方记一次不带组大小，接收方记组大小加 count） | kernel/probe.js:59 在途复用、kernel/store.js 广播 |
| 30 | dedup.hit | 调试 | scope 链或快照、keyHash 键散列 | 散列键 | — | 只记计数，窗口到才记一次 | kernel/probe.js:13 在途链、59 在途快照 |
| 37 | statusbar.hydrate | 调试 | cwdSource 工作区来源枚举 | 不记路径原文 | — | 采样 | statusbar/StatusBar.js:38 跟随水合 |
| 43 | error.normalize | 调试 | rawKind 原始类别、mappedKind 归一后类别、httpCode 状态码 | H_ERR | R_TOKEN_BEARER、R_GH_TOKEN | 按事件（只记归一后） | index.js:340 类别归一、tracker/errors.js |
| 44 | timer.schedule | 调试 | name 定时器名枚举、intervalMs 间隔、jitterMs 抖动 | — | — | 只记调度与跳过，不记每次心跳 | index.js:47 缓存 60 秒、api.js:46 改名轮询 5 秒 |
| 45 | privacy.scrub | 调试（纠偏移出常驻） | field 字段枚举、rule 规则名、hit 是否命中 | 自身不记原文 | 自身即计数，不记命中文本 | 按事件 | 全链路工作区路径、标题、令牌 |
| 52 | chain.cache.miss | 调试（#498 新增） | keyHash 键散列、lang 语言、reason 未命中原因（枚举 force、empty、key-changed、expired） | 散列键 | — | 按事件 | host/detectChain.js 链缓存穿透 |
| 53 | workspaceStore.miss | 调试（#498 新增） | keyHash 键散列、reason 未命中原因（枚举 empty、expired） | H_CWD | — | 按事件 | tracker/detection/workspaceStore.js 内存选择缓存 |
| 54 | client.snapshot.hit | 调试（#498 新增） | keyHash 工作区键散列、ageMs 缓存多久、kind 内存或磁盘 | 散列键 | — | 采样（百分之一，渲染路径高频） | kernel/store-snapshot.js 客户端快照表、kernel/probe-snapshot.js 磁盘命中 |
| 56 | detail.cache.hit | 调试（#498 新增） | numHash 票号散列、ageMs 缓存多久 | 散列票号 | — | 采样（百分之一） | kernel/api-io.js 详情 60 秒缓存 |
| 58 | exec.run | 调试（#606 新增） | argv0 命令名（只记程序名，连目录部分都去掉；参数一个不记）、cwdHash 工作区短指纹、latencyMs 耗时、exitCode 退出码（拿不到为 -1）、via 发起链名（枚举，取值见下） | H_CWD | —（命令名已去掉目录，只留程序名） | 外层判断（调试开关关闭时只读一次开关就返回，不读时钟也不组装字段对象） | host/platformChannel.js 的 detectionExec（操作上下文交给后端的 exec 就是它）与 host/repoKeys.js 的 execProc（少数自建上下文与初始化推送直连它）；两处互斥，每条外部命令只落一行 |
| 59 | panel.render | 调试（#606 新增；#646 补两个阶段名；2026-09-21 去掉 better-sidebar 那条路的两个阶段名，「打开面板」这一路不再记 mode） | stage 阶段名枚举（native-open 调用原生右侧栏控制器之前、native-opened 原生控制器返回之后、entry-unavailable 唯一那条路也不可用、render-commit 进入渲染到提交完成、render-paint 进入渲染到被动副作用跑完、fit-measure 提交阶段里折叠测量耗时、click-to-painted 点击到被动副作用跑完）、ms 该阶段的毫秒数、mode 哪条线在推进（只有更新面板那条线记 `update`；打开面板这一路 2026-09-21 起不记 —— 面板只有一条路，形态没有第二种取值） | —（只记枚举与数字） | — | 外层判断（调试开关关闭时直接返回，不组装字段对象） | 打开面板那一路：kernel/router.js 记起点与原生右侧边栏两段、panel/Dock.js 记渲染起点与提交、views/ListTab.js 累加折叠耗时、panel/DockSync.js 收口成行；另一条线是 views/useUpdatePanel.js 的更新状态推进 |
| 61 | labelColors.read | 调试（#618 新增） | cwdHash 工作区键散列、present 文件在不在、count 文件里有多少行标签、ok 是否读成功、reason 原因枚举（read-fail 读不出来、not-json 不是合法 JSON、not-object 不是对象、bad-entry 某一行的写法不对） | H_CWD | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象；列表刷新与票列表上色都会读它，属高频路径） | 本地 Markdown 房间读工作区配色文件的三个落点：列标签、保存前的读—改—写、票列表上色（src/host/tracker/backends/markdown/label-colors.js） |
| 62 | labelColors.panelPatch | 调试（#635 新增） | cwdHash 工作区键散列、kind 这一次是哪一种（applied 保存成功当场把后端确认的颜色写进面板那份快照 / kept 装进来的快照比那一笔记录旧、按记录补色 / expired 装进来的快照比记录新、记录作废）、count 条数（applied 与 kept 记这一次写动的处数；expired 记作废掉的那笔记录里收着几个标签） | H_CWD | — | 按事件（只有真写动或真作废才记；一次保存最多一条，装快照时才可能再来一条，低频） | 保存成功后的面板显示链路：客户端内存里那笔「这次保存确认过的颜色」记录（src/client/views/labels/labelColorPatch.js，落点两处：保存成功当场写、快照装进面板时补） |
| 63 | workspaceRoot.resolve | 调试（#652 新增） | cwdHash 会话所选目录的散列、rootHash 判定出来的工作区根散列、source 靠什么标记停下来（枚举 git 这一层自带 .git / anchor 这一层自带主锚文件 docs/agents/issue-tracker.md / self 一路到顶都没有标记所以用所选目录本身 / fail 探测失败回退所选目录 / no-fs 与 no-probe 是连探测能力都没有 / relative 不是绝对路径不判 / empty 空入参）、cache 这一次缓存什么结果（枚举 hit 命中 / miss 未命中 / expired 过期重算 / skip 没算） | H_CWD（两个散列，不记路径原文） | R_WIN_ABS、R_HOME_PATH | 外层判断（同一行先判调试开关；缓存命中按百分之一采样，未命中与过期每次都记；统一钥匙的必经之路，属高频路径） | src/host/workspaceKey.js 的 resolveWorkspaceRoot（#652 新增的工作区根判定与它那层 30 秒内存缓存） |
| 64 | inject.decision | 调试（#655 新增；2026-09-21 #669 第 6 件扩了一个 prompt 取值；2026-09-22 #698 又扩了两个 kind 取值、一个 prompt 取值，并把落点搬去 kernel/prompts-setup.js） | prompt 这次决定的是哪一段（枚举 setupRun 初始化全文 / switchAlign 换过后端之后那条「把仓库里记录后端的那几处对齐」的指令 / switchSettle 切换那条路上「卡答完之后给出去的是哪一类」） | kind 决定了哪一种（枚举 setup 注入初始化全文 / askLayout 布局还没定、先开那张小卡问一句、不注入（#698 前叫 setup-card，黄条那条路与切换那条路开的是同一张卡，故统一成一个名字）/ blocked 清单里标着「没过就不许初始化」的那一步还没过、一个字都不注入（#664 起；原先那个 repo 缺仓库改发建仓指引的取值随新流程退役）/ align 换过后端、注入的是那条对齐指令（#669 第 6 件新增，只在已初始化的工作区里出现）/ align-layout 除对齐后端之外，还多给了一条把布局那一句对齐到新结论的指令（#698 新增，只在用户在卡上真的改了布局时出现）） | layout 按哪种布局（枚举 single 根目录一份 CONTEXT.md / multi 子项目各一份 CONTEXT.md，根目录 CONTEXT-MAP.md / unset 还没选；switchAlign 那条与布局无关恒记 unset，switchSettle 记这次会话里的结论） | —（只记枚举，不记仓库、路径与文案原文） | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象；用户点击触发，一次操作最多几条，低频） | src/client/kernel/prompts-setup.js 的 injectSetupDecision（#698 起这一段决策住这里）与 logSwitchSettle（切换那条路的收尾轨迹，也住在同一个文件：statusbar/ 目录里不许新开日志点）；另有一处落点仍在 src/client/kernel/store-switch.js（卡没开成时直接注入 switchAlign 那条兜底） |
| 66 | chain.stale.drop | 调试（2026-09-21 新增） | keyHash 这一次请求的链共享键散列（工作区键 + 后端 + 语言，只记散列不记路径） | H_CWD 同一套短散列 | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象；这一次回包不算数时才走到 —— 同一个键上又发过更新的一次，或这个会话已经不要这条链了，见 1.3 的更正说明） | src/client/kernel/probe-chain.js 的 loadChain（链快照回包处：晚到的旧结果不写进会话状态，见 1.3 的增补说明） |
| 67 | snapshot.stale.drop | 调试（2026-09-21 新增） | keyHash 这一次请求的工作区键散列（只记散列不记路径） | H_CWD 同一套短散列 | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象；这一次回包不算数时才走到 —— 这个工作区上又发过更新的一次，或这次问的后端已经不是用户现在选的那个后端，见 1.3 的增补说明） | 判断在 src/client/kernel/probe-stale.js（`_snapRespStale`），落点在 src/client/kernel/probe-snapshot.js 的 loadSnapshot（面板快照回包处：晚到的旧结果不写进会话状态。同一处还改了去重键：工作区 + 后端） |
| 71 | choiceStore.read | 调试（#683 新增） | keyHash 工作区键散列、outcome 这一次读到没有（枚举 hit 读到一条 / miss 没有这条） | H_CWD 同一套短散列 | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象；每次探测都经过的高频路径） | src/host/choiceStore.js 的 getWorkspace 与 getLayout |
| 72 | choiceStore.hint.reject | 调试（#683 新增） | keyHash 工作区键散列、baseRev 客户端手里那版的修订号、rev 宿主记的那版的修订号 | H_CWD 同一套短散列 | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象；只在「客户端手里那版比宿主旧」时走到） | src/host/tracker/detection/detectionService.js 的 detect（按宿主发号的修订号判谁说话那一档） |
| 73 | choiceStore.evict | 调试（#683 新增） | count 这一次丢了几条、max 当前条数上限 | —（只记两个数字） | — | 外层判断（同一行先判调试开关，关闭时连判断都不做；超过上限才走到） | src/host/choiceStore.js 的两条写路（后端记录与布局各一处） |
| 75 | quota.spend | 调试（#706 第二批新增） | account 记在哪一档（枚举 plugin 插件自己的刷新 / user-action 人在界面上亲手做的动作 / ai-tool AI 工具调用）、bucket 记在哪个桶（枚举 rest 数请求条数 / graphql 数点数）、requests 这一笔几条真实出站请求（分页、重试、兜底链、扇出都算进去后的条数）、points 这一笔几点、remaining 记完这一笔之后这一桶的剩余额度（保守下界：服务端读数减本地已花，与这一份额度自己还剩多少取小者） | H_CWD 同一套短散列（工作区键只作为记账的分桶，不进日志） | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象；每一笔真实出站都会走到，属高频路径） | src/host/refresh/ledger.js 的 spend（闸每记一笔花费落一行；读服务端剩余额度那一条也在这里落，记成 quota-read 那一笔） |
| 76 | refresh.decide | 调试（#706 第二批新增） | category 四个类别之一（枚举 user-action 人的动作 / lifecycle 切进工作区那一次 / background 后台定时活 / ai-tool AI 工具）、kind 请求种类（枚举，取值表见 policy.js 的 REQUEST_KINDS）、verdict 结论（枚举 allow 放行 / degrade 降级 / defer 推迟 / reject 拒绝）、reason 原因代号（枚举，取值表见 policy.js 的 REASONS，日志里只记代号不记那句大白话）、tier 当时这一桶的档位（枚举 green / yellow / red） | —（只记枚举） | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象；每一笔经闸的请求都落一行，AI 工具被拒那条路也落） | src/host/refresh/gate.js 的 fireDecide（send 与 admitAiTool 两处都调它） |
| 77 | refresh.skipped | 调试（#706 第二批新增） | keyHash 工作区键散列（只记短散列，绝不记路径原文）、kind 请求种类（同上枚举）、reason 原因代号（枚举；推迟记 policy.js 给的原因，5 分钟过期丢弃记 defer-expired，AI 工具被拒记它那三条原因之一）、pending 落这一行时推迟队列里有几条 | H_CWD 同一套短散列 | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象；只在真被跳过时走到 —— 后台档推迟、额度不足、推迟队列过期丢弃、AI 工具被拒） | src/host/refresh/gate.js 的 fireSkipped（推迟入队、过期丢弃、AI 工具被拒三处都调它） |
| 81 | write.event | 调试（#710 第二批新增） | keyHash 工作区键散列（只记短散列，绝不记路径原文）、shape 哪条订阅路送来的（枚举 tools/result 运行时事件 / tool/result 会话事件 / tool/ptc-dispatch 内层派发的收尾 / tool/code-dispatch 磁盘旧名）、tier 判成哪一档（枚举 write-confirmed 确定写 / probe-now 可疑、立刻探一次 / default-tick 其余、等兜底节拍）、reason 原因代号（枚举，取值表见 src/shared/refresh/write-detect.js 的 DETECT_REASONS，日志里只记代号不记那句大白话）、action 接下来做什么（枚举 patch-now 只补票号指的那一行 / probe-now 立刻探一次 / wait-tick 什么都不额外做）、hasTicket 认没认出票号（布尔；**票号本身、命令原文都不记**） | H_CWD 同一套短散列 | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象）+ 两条节流（兜底那一档根本不记；同一工作区一秒内只记第一条）。每一次成功的工具调用都会经过它，属高频路径 | src/host/refresh/writeEvents.js 的 logFired（两条订阅路合用的那一处：tools/result 为主动，session/event 为补充） |
| 82 | refresh.patch | 调试（#708 第三批新增） | keyHash 工作区键散列（只记短散列，绝不记路径原文）、mode 这一次走哪一档（枚举 patch 只补那几条薄查询 / rebuild 该整池一次 / nothing 探回来没变 / deferred 被闸推后 / failed 出错 / stale-drop 这份结果已过期被丢）、reason 原因代号（枚举，取值表见 src/shared/refresh/delta.js 与 src/host/refresh/patch.js：rows-changed、no-baseline、ticket-set-changed、too-many-rows、legacy-structure、no-watermark、merged、deferred-kept-watermark、dropped-kept-watermark、failed-kept-watermark、no-change、stale-result 等）、changed 这一次变了几行（数字）、queries 真发出去几条薄查询（数字；被推后与该整池时都是 0）、watermark 水印推没推（枚举 advanced 推了 / held 没推）、pending 这一行落下去时待办里几条（数字） | H_CWD 同一套短散列 | —（不记路径、不记命令行、不记票标题） | 外层判断（同一行先判调试开关，关闭时不组装字段对象；挂在每一次探测之后 —— 活跃工作区 5 秒一拍那条路，属高频路径） | src/host/refresh/patch.js 的 fire（补行、该整池、没变、被推迟、出错五条分支都调它；水印与待办就在这份文件里维护，跟着快照缓存那一份文件落盘） |
| 78 | attention.report | 调试（#707 第三批新增） | kind 这一次是哪种上报（枚举 focus 窗口获焦 / panel-open 面板打开 / session-switch 会话切换 / heartbeat 面板可见时每 20 秒一次的本地心跳 / window-blur 窗口失焦 / panel-hidden 面板被藏起来）、windowHash 窗口标识散列（客户端自己造的标识，只记短散列）、rootHash 工作区根散列（绝不记路径原文）、visible 面板此刻可不可见（布尔）、accepted 这一笔算不算「我正在看」的新信号（布尔；不可见的心跳、失焦与隐藏都不算） | H_CWD 同一套短散列（窗口与工作区都只记散列） | —（全是枚举、散列与布尔） | 外层判断（同一行先判调试开关，关闭时不组装字段对象）+ 采样（心跳每 20 秒一次，按十取一） | src/host/refresh/attention.js 的 fireReport（宿主电话 wf.focus 的 handleFocus 里调它） |
| 79 | attention.sweep | 调试（#707 第三批新增） | active 活跃集合里现在几个根（数字）、keep 该有定时器的几个（数字）、cancel 该停掉的几个（数字）、linger 有没有一个限时收尾节拍（布尔）、collaped 整桌收摊了吗（布尔）、keepers 「正在看」的根有几个（数字，不含刚离开的那个） | —（只记数字与布尔） | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象；收摊是节拍级动作，低频，不采样） | src/host/refresh/attention.js 的 fireSweep（sweep 里调它） |
| 84 | chain.cache.write | 调试（#709 登记收口新增） | keyHash 链共享键的短散列、allGreen 这份快照是不是每一步都 done（布尔）、doneCount 这一步之前链上已有几步 done（数字）、step 写回之后退到第几档（数字）、progressed 这一轮算不算有进展（布尔：多出一步 done 才算，重跑一次拿到一样的结果不算） | H_CWD 同一套短散列 | —（全是散列、枚举与数字） | 外层判断（同一行先判调试开关，关闭时不组装字段对象；每次求值都会写回一次，属高频路径） | src/host/refresh/chainBackoff.js 的 note（#709 新加的退避态是一份内存缓存，这一行是它的写回记录） |
| 85 | chain.event | 调试（#709 登记收口新增） | reason 这一次是哪种事件带上来的（枚举 enter-workspace 切进工作区 / user-recheck 人点「重新检查」/ action-done 做完可能改变它的动作 / write-done 写入成功之后） | —（只记一个枚举） | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象） | src/client/kernel/probe-chain.js 的 chainEventRefresh（从前那条 8 秒自轮询退役之后，客户端那几路事件都走这一个入口） |
| 86 | naming.guard.event | 调试（#709 登记收口新增） | reason 这一次是哪种来路（枚举，与 #85 同一串取值，兜底记 event） | —（只记一个枚举） | — | 外层判断（同一行先判调试开关，关闭时不组装字段对象） | src/client/kernel/api-naming.js 的 namingGuardianEvent（从前那条 5 秒命名轮询退役之后，四种事件到来时催一次的那一下） |
| 87 | chain.preflight.reuse | 调试（#709 登记收口新增） | cwdHash 工作区短散列、checks 这一轮真问出去几条预检命令（数字）、reused 同一轮里省掉了几次外部问答（数字；大于 0 才说明复用真的发生了）、userAction 这一次是不是人亲手点的（布尔） | H_CWD 同一套短散列 | —（不记命令行原文、不记路径原文、不记任何返回值） | 外层判断（同一行先判调试开关，关闭时不组装字段对象；每次求值只落一行） | src/host/detectChain.js 的 handleChain（一次求值的环境预检收尾：登录态、仓库可达这两条各只真问一次）；复用位的实现与计数在 src/host/tracker/detection/preflightScope.js |
| 89 | snapshot.late.install | 调试（#727 新增） | keyHash 请求发出时那条目录的工作区键短散列、installed 这一小块（选择与仓库引用）真的补装上了没有（布尔） | H_CWD 同一套短散列 | —（不记路径原文、不记回包内容） | 外层判断（同一行先判调试开关，关闭时不组装字段对象；走到这里本身就是少见的一件事 —— 那次取数已经等超时了、回包后来才到） | src/client/kernel/probe-select.js 的 _installLateSnapshotSelection（迟到回包里那一小块的落地口） |

### 1.6 自监控 5 条（#499，错误与告警级、始终落盘，不采样不节流之外的节流见各行守卫）

自监控行回答一个问题：日志管道自己出故障时，盘上有没有留下它自己的供词。五行的触发点都在管道必经之路上，字段只用枚举、散列与计数，不记任何原文。

| # | 事件名 | 级别 | 允许字段（白名单，之外不记） | 截断或散列 | 命中正则名 | 守卫 | 落点 |
|---|---|---|---|---|---|---|---|
| 46 | host.dispatch.error | 错误 | method 电话名、argsHash 入参散列、errorKind 归一类别 | H_ERR（入参只记散列） | — | 无外层判断（错误直通）；业务性失败回参不触发，只记未捕获抛错 | src/host/rpcChannel.js 分发 catch（#596 由 src/host/index.js 搬来；通道路径注册失败也记在此，method 写通道路径、errorKind 见 1.6 末尾枚举注） |
| 47 | log.persist.fail | 告警 | op 操作枚举、reason 原因枚举、dirHash 目录散列 | H_CWD（目录只记散列） | — | 无外层判断（告警直通）；同轮合并、同一行在途未落定不再追加，避免失败自我繁殖 | src/host/logStore.js 写盘与开关持久化失败分支 |
| 48 | log.forward.summary | 告警 | droppedDelta 新增丢弃数、totalDropped 累计丢弃数、reason 原因枚举、windowMs 统计窗口毫秒 | —（只记数字与枚举） | — | 无外层判断；只在批量发送完成后、有新增丢弃时记一行，不逐条 | src/client/kernel/log.js 转发完成后 |
| 49 | log.switch.watchdog | 告警 | op 操作枚举、timeoutMs 超时毫秒、stage 所处阶段枚举 | —（只记枚举与数字） | — | 无外层判断；单次操作只记一行，超时后迟到的回包不补记 | src/client/kernel/log.js 开关写与对账的竞跑计时 |
| 50 | log.export.fail | 告警 | op 操作枚举、reason 原因枚举、errorHash 错误散列 | H_ERR、T120（先 120 字截断再散列） | — | 无外层判断；用户手势触发才可能记，无轮询无后台自发 | 状态栏菜单与设置页导出链路失败分支 |

枚举取值（写死在这张表里，实现与门禁逐字锁死）：op 在 47 取 writeBatch、persistSwitch、readBack，在 49 取 set、reconcile，在 50 取 export、openDir、copyPath、resolve；reason 在 47 取 no-dir、read-fail、write-fail（建目录失败无法与目录已存在区分，仍走静默吞，由随后的写结果说话），在 48 取 queue-full、packet-trim、send-fail、host-reject，在 50 取 host-unavailable、export-not-ok、path-missing、open-fail、copy-fail；stage 在 49 取 waiting-host；errorKind 在 46 取 auth、network、notfound、exit，归不上记 internal（#596 起：客户端与宿主之间那条通话通道注册失败也记在 46，原因归不上上面四类，一律记 internal，是哪种失败写在 method 尾巴里）。

### 1.7 退役电话（#498，走 #22/#34 先例，只记追溯不埋点）

- wf.ping：删宿主注册（src/host/index.js），全仓零调用点、从未有专属事件；探活改走免参开关读电话 wf.logGetSwitch；handlePing 实现留守；冒烟探活已改走新电话。
- wf.claim：删宿主注册（src/host/index.js），全仓零调用点、从未有专属事件；认领取走 wf.handoffResolve；handleClaim 实现留守。
- 定时器名现状（2026-09-24 订正）：timer.schedule 的 name 只许 naming-sweep、naming-persist 两个；#709（T5）把三条自续循环整条拆掉，随之退役的三个名字是 naming-guardian（宿主侧命名守护的 15 秒自续 tick）、naming-poll（客户端命名轮询）、checks-poll（检查页那条 20 秒静默重查）；#725 又把状态栏胶囊那台 2 秒折叠重算拆成「ResizeObserver + 每次提交后重算一次」，statusbar-poll 这个调度名一并退役（它原先只是为那台轮询记的调度行）。这四个名字都不许再出现在调度行里（`tests/verify-log-coverage.js` 反向锁死，全库「不许有自续定时器」由 `tests/verify-709-no-self-continuing-timers.js` 钉死）。有界一次性重试（如侧栏注册重试最多 10 次）、瞬态倒计时（如交接搜索秒级 tick）、纯界面延时（如 toast 清除）不记调度行，见覆盖门禁豁免表。

## 2 通道批量量化：批量大小、上报间隔、单包上限与失败降级

### 2.1 先说什么叫通道、批量、单包、降级

- 通道的意思是客户端把日志批量转发给宿主统一落盘的那条路。客户端自己不写盘，只把日志行攒一批发给宿主的新电话，由宿主写当天文件。
- 批量的意思是攒够多少条或等了多久就发一次。单包的意思是一次发的最大体积与条数，超了就截断。降级的意思是通道失败时怎么办。

### 2.2 建议值（#335 可直接引用）

- 批量大小：每批最多 50 条。理由：一次快照组装加一次面板打开的常驻量约几十条，50 条能把一次用户操作的现场装进同一包，又不会让单包过大。
- 上报间隔：每 1000 毫秒发一次。理由：沿用命名守护把多次变脏合并为一次写盘的做法（1200 毫秒防抖），1000 毫秒是同数量级的等待，既合并了高频抖动，又让崩溃窗口不超过 1 秒太多。批量未满也按时间发，批量先满则提前发。
- 单包上限：128KB 或 100 条，先到先截，超了截断并在包尾记一条截断标记。理由：128KB 远小于子进程输出上限（标准输出 4MB、标准错误 256KB 的历史证据），一次转发不会挤占快照电话；100 条封顶让宿主单次写盘体积可预期，按天分文件后单次覆写代价不随运行时间递增。
- 文本组装位置：所有字符串拼接与对象转文本只在调试开关判断通过后做，渲染路径不直接转文本（沿用 #330 客户端结论）。

### 2.3 通道失败降级语义三选一并定版

- 选项一：丢弃并计数。通道失败或队列满时丢掉新日志，只把丢了几条记成计数器，下次成功时顺带上报。
- 选项二：本地缓冲。客户端把发不出去的日志暂存浏览器内存，等通道恢复再补发。
- 选项三：背压。调用处等通道恢复再返回，日志不丢但调用处被拖慢。

推荐选项一（丢弃并计数），理由：

- 日志不能拖慢快照、探测、命名守护等主电话，背压会把写盘延迟污染主流程，不采用。
- 本地缓冲在面板 долго开着时会无界长大，违背关闭时零分配，内存压力反而制造新的现场，不采用。
- 丢弃并计数与现有写失败只计数不抛错一致（命名守护与快照缓存都是失败静默、留到下一轮），也与单写者队列繁忙时合并等待一致；计数本身作为可观测事件，能告诉维护者丢了多少，而不是假装没丢。
- 具体做法：客户端队列满（按单包上限）或转发失败时丢新日志并累加 droppedCount；宿主写盘失败同样只计数不抛错；droppedCount 随下一包上报，包里只记数字不记原文。

## 3 刷盘规则：直通、双进程同写、目录漂移对账

### 3.1 错误与告警直通刷盘（崩溃窗口不丢致命日志）

- 错误与告警不进批量合并等待。入队后立即触发一次刷盘（取消本次防抖等待，本轮事件循环末就写当天文件），但调用处仍只进内存队列就返回，不等写盘完成。
- 日常信息中常驻 31 条走批量（1000 毫秒窗口合并），按需 27 条只在调试开关打开时进批量。
- 目标：崩溃窗口只剩进程崩溃到写盘之间的毫秒级，致命日志不因为等批量而丢失；正常时批量仍把单次覆写体积封顶在一天之内。

### 3.2 双进程同写规则（同一台电脑开两个宿主进程）

- 单个宿主进程内所有写入排成一队一次只写一份（单写者队列加繁忙守卫），写失败只计数不抛错。
- 跨进程不加锁：两个宿主进程写同一个当天文件时，以后写为准，前一次可能被覆盖，审计行可能少记。该行为与现有快照缓存一致，本轮不解决，需要时另立任务票（沿用 #330 宿主假设 3）。
- 每次启动在日志头写入进程标识、启动时间与实际目录，事后能区分哪几行来自哪个进程，不靠锁来区分。

### 3.3 进程当前目录漂移与启动对账

- 缓存目录跟着宿主进程当前目录走（当前解析办法见第 5 节证据），宿主换一种启动方式导致当前目录变化时，日志目录跟着变，旧日志不自动迁移（与现有缓存的已知局限相同）。
- 启动对账要求：启动时解析一次实际缓存目录并写入日志头（实际目录、启动时间、进程标识）；客户端启动先读本地开关秒显，再向宿主读开关对账，对账前不产生调试日志；对账以宿主为准，重启后保持（沿用 #332 定版）。
- 导出时若发现存在多个日期目录或多个历史目录并存，提示用户一并打包，并在包里附目录清单，不静默只导当前目录。

## 4 #335 落地门禁断言草案（守卫形态写成自动判定，不过就阻塞发布）

守卫形态的意思是把上面的清单写成自动判定脚本（tests/verify-log-*.js），加入发布前检查，不过就阻塞发布，不靠人工去看。#335 落地时把本节逐条实现为脚本，本附录只定断言文字。

- 断言一：字段白名单。扫描全部埋点调用，断言每个事件只含第 1 节允许字段；出现工作区原始路径、仓库地址原文、令牌原文、模板正文、快照全文即红。
- 断言二：外层是否开启判断。扫描高频路径（自动探针、扇出、差异、水合、链秒显、在途复用、状态栏水合）的日志调用，断言调用前有外层是否开启判断；无判断即红。日志函数体内仍保留内部拦截做兜底。
- 断言三：截断。扫描标题与错误组装，断言标题必经留前 80 字、错误必经留前 120 字（详情 160 字）并标注已截断；直接拼接或直接转整行对象即红；渲染函数体内出现日志用对象转文本即红。
- 断言四：错误与告警直通。扫描错误与告警调用，断言走直通刷盘路径；进入批量合并等待即红；调用处等待写盘完成即红（必须只进队列就返回）。
- 断言五：正则名。扫描自由文本掩码，断言命中只记第 1.2 节具名正则名；出现未知规则名、命中记原文、未命中扩写表达式即红。
- 断言六：计数。断言常驻 31 条、按需 27 条、自监控 5 条、总数 63（#22、#34、#35 已退役，见 1.3 落定说明与 1.4 的退役说明；#498 新增 #51、#52、#53、#54、#55、#56，#548 新增 #57，#606 新增 #58、#59，#618 新增 #60、#61，#635 新增 #62，#652 新增 #63，#655 新增 #64，#663 新增 #65，见 1.3 增补说明）；增删事件必须同步更新本附录对照表，否则红。断言编号清单与本节一致（常驻含 #9、#38、#41、#51、#55、#57、#60、#65，不含 #45，不含已退役的 #22、#34、#35；按需含 #45、#52、#53、#54、#56、#58、#59、#61、#62、#63、#64；自监控为 #46～#50，见 1.6 节）。
- 断言七：通道数量。断言客户端批量转发为每批最多 50 条、每 1000 毫秒、单包 128KB 或 100 条先到先截；通道失败处理为丢弃并计数；出现背压等待或无界缓冲即红。
- 断言八：双产物。断言新增电话名在开发产物与打包产物同时存在（沿用双源镜像门禁写法），只改一处即红。
- 断言九：自监控。断言 46～50 五个事件在源码逐个有点名、级别全为错误与告警、字段全在 1.6 节白名单内、告警行无外层是否开启判断（复用断言四口径）；看门狗超时字面为 5000 毫秒；落盘失败行带防繁殖守卫；实现见 tests/verify-log-selfmon.js。
- 断言十：覆盖（#498 新增）。每个非退役电话至少一行方法可识日志发射（成功常驻信息、失败常驻告警、高频成功按需调试加守卫）；每个客户端调用点可追踪（相邻行或被调电话的方法可识行覆盖，动态透传点单列断言）；五类动因（跨边界调用、新增电话、面板读链路、缓存、定时器）无日志点即红；退役电话（wf.ping、wf.claim）重现注册即红；日志五电话成功行豁免、失败行不豁免。实现为 tests/verify-log-coverage.js。

## 5 证据索引（行号以本分支基线为准）

- 术语六词条：CONTEXT.md:85-92（日志、调试开关、落盘、导出、脱敏、性能守卫，2026-09-05 定版）。
- 词典订正：CONTEXT.md:87（常驻 29 条纠偏括号与按需 16 条，基线已含任务一订正）。
- 缓存目录解析：src/host/repoKeys.js:154-159（进程当前目录下新建独立缓存目录，建目录不存在也不报错）。
- 命名守护防抖与节拍：src/host/namingGuardian.js:67（变脏后 1200 毫秒才写盘）、:23（常驻循环每 15 秒）。
- 电话注册形状：src/host/index.js:215-216、242、244（宿主用内存表登记 wf. 开头方法，日志需新增批量记录电话）。
- 快照缓存短语：src/host/index.js:9（快照 5 秒缓存，高频守卫依据）。
- 开关本地存储：src/client/kernel/config.js:9（键 dsws.cfg）、:27（保存即写本地存储）。
- 标题截断榜样：src/client/kernel/api-io.js:15（标题留前 80 字）。
- 第 1 节原证据列转引 #331 旧基线 main@84c6594，只作追溯；本附录新增数量与做法的行号证据以上本节为准。

## 6 假设清单（未知先记下来，不默认）

- 落盘配额未知：没有找到官方配额声明，本附录按手动清空兜底，不做自动轮转，与地图范围一致。
- 文件服务没有追加原语：仓库里只出现过全文读与全文写，所以推荐按读改写加按天分文件；若官方其实有追加能力，落盘代价可再降一档。
- 双进程覆盖不解决：以后写为准，审计行可能少记，与快照缓存一致。
- 目录漂移不迁移：旧日志不自动跟过去，靠启动头与导出提示区分。
- 电话参数形状与脱敏责任划分的最终字面留给 #335；本附录只定字段与规则，不定函数签名。
- 批量数值（50 条、1000 毫秒、128KB）为建议初值，需真机验证后微调；调整时同步更新第 4 节断言七。
- 退役落定（#494，2026-09-06）：#22 与 #34 退役后不再找新锚点。依据是基线 origin/main@771bc04 全仓验证：宿主侧事件队列与轮询电话已清零、客户端侧记录函数只剩无人调用的空壳、无任何日志发射点输出这两事件。将来若重做同类定位能力，另立票重定事件名与白名单，不复活这两个已退役行。
- 正则清单为初版，未命中不扩写表达式，由白名单兜底；随误报迭代时同步更新断言五。
- 自监控落定（#499）：46～50 五个新编号独立于 45 编号之外，不复用旧编号、不复活已退役的 #22 与 #34。原因是旧编号各自绑定固定字段形状，硬复用会污染白名单语义；#27 是客户端视角的调用失败，与宿主分发视角的抛错各记各的，两端靠电话名对账。

*真实换行、无字面反斜杠 n、无 BOM。引用行号以本分支基线 origin/main @905762e 为准。*- 增补说明（2026-09-22，#698「切换后端时每次都先问一次域文档布局」）：**没有新增事件，只给已有事件 #64 `inject.decision` 扩了两个 kind 取值、一个 prompt 取值，并搬了一次落点**，所以三条名单与条数照上一段不动（常驻 35 条、按需 31 条、自监控 5 条、总数 71 条）。逐条说清：① **落点搬家** —— 这一段决策（`injectSetupDecision` 与「该不该先开那张布局小卡」的判据）从 `src/client/kernel/prompts.js` 搬去了 `src/client/kernel/prompts-setup.js`（prompts.js 贴着 350 行上限），字段与开关纪律一个字没变；② **prompt 多一个取值** `switchSettle` —— 切换后端那条路从这一票起会分成「先开卡问布局 / 直接对齐后端 / 多给一条布局对齐 / 什么都没给」四种，原来那一行看不出「在等你答卡」与「卡根本没渲染出来」的区别；③ **kind 多两个取值** `align`（只对齐后端）与 `align-layout`（除对齐后端之外，还多给了一条把布局那一句对齐到新结论的指令）；④ `setup-card` 这个取值改名成 `askLayout` —— 黄条那条路与切换那条路开的是同一张卡，却曾有两个名字（`setup-card` / `setup-card-switch`），现统一成一个；⑤ 原有的 `prompt: 'switchAlign'` / `kind: 'align'` 那一处落点仍在 `src/client/kernel/store-switch.js`（没开成卡时的兜底那条）。**级别仍按需**：这一条由用户点击触发、一次操作最多几条，外层先判调试开关；要不要转常驻已写进 #698 票面等维护者拍板（按日志纪律的形状它倾向常驻，代价是台账连动）。
