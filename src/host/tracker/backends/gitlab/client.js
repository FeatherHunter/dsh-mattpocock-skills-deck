/**
 * backends/gitlab/client.js — glab CLI 封装。
 *
 * ⌈ 骨架占位 ⌉ #116 实现：resolveExecutable('glab') + 超时 + 错误归一化。
 */

export function glabClient(ctx) {
  void ctx
  return {
    async run() {
      throw new Error('glab client pending #116')
    },
  }
}
export default glabClient
