## 一、这一轮做到哪一步（先说结论）

- **本地 Markdown 那一侧：三问都已实测出结论**，是第一手实测，不是读文档推的（下文第四节给出复现办法与机器输出）。
- **GitLab 那一侧：只有「读代码 + 查官方文档」得到的静态事实，四条要在真机上才能回答的问题还没答**（下文第五节）。本机没有 GitLab 环境：`glab` 命令不存在，没有可连的 GitLab 仓库。
- 因此这张票**还不能关**：它要的交付物是「判据与命令写进评论 + 实测结果写进评论」，GitLab 那一半的实测结果还不存在。下文第五、六节已经把**判据、逐条命令、要留的证据、什么算通过**全都写死，谁有环境谁照抄即可。
- 本地 Markdown 那三条结论已经足够让「落地：阶段 3」动手写它的 `counts` / `listPage`，不必再等；GitLab 那两条仍要等实测。

## 二、本地 Markdown 的三问（已实测，可据以落地）

### 2.1 数什么：数「列举会返回的那些票」，不是数目录里的文件

一道实测的对照表（工作区 `<cwd>/.scratch/demo/`，`issues/` 里放 4 个票文件加 1 个 `notes.md`）：

| 怎么数 | 数出来 | 与票数的差 |
| --- | --- | --- |
| `issues/` 目录里所有 `.md` 文件 | 5 | 多 1（`notes.md` 不是票，但也是 `.md`） |
| 文件名以数字开头（`^\d+-`）的 `.md` | 4 | 正好等于票数 |
| 列举操作返回的行（含地图容器行） | 5 | 多 1（地图 `map.md` 自己也占一行） |
| 列举操作返回的票行（不含地图容器） | 4 | 正好等于票数 |

`src/host/tracker/backends/markdown/issues-read.js:37` 与 `:61` 的筛选规则就是「以 `.md` 结尾，且文件名以数字加短横开头」；不符合这条的文件**既不出现在列表里，也不该出现在数字里**（它不是票）。

**建议口径**：`counts` 数「列举操作返回的行」，按 `type` 分成地图与票，按状态分成开放与已关闭；`open`/`closed`/`total` 三个数按与宿主相同的口径给出。这样「数字」与「行数据」同源，不会出现「数字说 100、列表只画 98」这类对不上的情况。

### 2.2 状态怎么分：由票文件里那行 `Status:` 决定

实测（`src/host/tracker/backends/markdown/parse.js:37-40`）：`Status:` 的值落进 `resolved` / `completed` / `closed` / `done` 四者之一算**已关闭**，其余一律算**开放**；没有 `Status:` 行的也算开放。

带状态筛选问同一个工作区，结果与逐文件解析完全一致：

| 问法 | 返回行数 | 其中票 | 开放 | 已关闭 |
| --- | --- | --- | --- | --- |
| 不带筛选（全仓库，两个工作单元） | 7 | 5 | 2 | 3 |
| 只看 `demo` 这一个工作单元 | 5 | 4 | 2 | 2 |
| 只要 `state=open` | 3 | 2 | 2 | 0 |
| 只要 `state=closed` | 2 | 2 | 0 | 2 |
| 只要票不要地图（`parentKey='00'`） | 4 | 4 | 2 | 2 |
| 只要地图（`parentKey=null`） | 1 | 0 | 0 | 0 |

两条要注意的口径：

- **带 `state=open` 时地图容器行仍然返回**（它自己是开放状态）。若 `counts` 要照搬筛选，就要先把地图行与票行分开，别把地图行算进「开放票数」。
- **不带筛选时是所有工作单元一起数**（`alpha#01` 与 `demo#01` 各算一张）；带 `effortId` 时只看那一个。这就是规格第 4.1 节说的「多工作单元的后端能按工作单元分别数」，落地时按 `repo.effortId` 作用域即可，不需要新机制。

### 2.3 翻页：文件列表切片，游标就是下标；当前列举这一次读已经拿全

`issues-read.js` 的列举是「把该工作单元目录下所有票文件逐个读出来再过滤排序」，**今天就是全量读出**（规格第 5.2 节的判断属实）。所以 `listPage` 没有额外代价，就是给这份排序好的列表切一刀：

- 游标 = 切到第几个（下标），原样回传；后端数据变了或进程重启后下标仍有效（这是它比 GitLab 游标更耐用的地方，不会失效）。
- 排序键要与列举保持一致，否则翻页会重复或漏行。今天列举的排序是「先按工作单元标识，再按编号」的字符串序（`issues-read.js:118`），**不是**规格第 4.2 节要求的「创建时间倒序」。落地时要么把列举也改成创建时间倒序（会改动现有列表的顺序，属额外影响），要么在 `listPage` 里按创建时间倒序重新排一遍（只影响按页取的那条路）。**这一条要维护者拍板**，见第七节。

### 2.4 票文件坏掉时报什么：**今天报不了错，因为一律不报错**

这是三问里最要紧的一条，与票面原来的假设**不一样**，据实记下：

解析这一层极宽（`parse.js` 用的是「取到就用，取不到给默认值」的写法，没有任何一处会抛），实测四种坏文件**全部照常变成票行**，一张都没报错：

| 文件内容 | 结果 |
| --- | --- |
| 空文件 | 变成一行，标题是空串 |
| 二进制垃圾（含 `\0` 控制字符） | 变成一行，标题就是那段乱码 |
| 只有 `Status: resolved` 与 `## Comments`、没有标题行 | 变成一行，标题成了 `Comments`，状态算已关闭 |
| 正常文件 | 正常 |

**真正会静默丢票的是「读不动」**，不是「内容坏」。实测：让某一张票文件读的时候抛错（权限不足 / 读到时已被删 / 被别的程序锁住），列举**照样回 `ok`**，只是少了一行，一声不响（`issues-read.js` 第 43、67、86 行那三处空 `catch`）：

| 工况 | 目录里实际有几张票 | 列举回来几行票 | 报错了吗 |
| --- | --- | --- | --- |
| 谁也不抛 | 4 | 4 | — |
| `03-c.md` 读不动（权限不足） | 4 | 3 | 没报错 |
| `03-c.md` 读到时已被删 | 4 | 3 | 没报错 |
| `03-c.md` 被别的程序锁着 | 4 | 3 | 没报错 |
| 两张票都读不动 | 4 | 2 | 没报错 |
| 地图文件 `map.md` 读不动 | 4 | 4（地图行没了） | 没报错 |

**落地建议**：把这条路的错误**如实报出来**，而不是继续丢。理由与规格一致——规格第 9 节对本地 Markdown 明确要求「后端如实报错，不静默丢票」，而今天恰恰是静默丢。具体做法：把「列出票行」这段抽成一个函数，列举与计数共用，遇到读不动的文件时把错误收集起来；只要有一个文件读不动，`counts` 就按规格第 4.1 节的失败语义回失败（`env` 或 `parse`，消息里说清是哪个编号的文件），**绝不回一个偏小的数**——否则界面会拿这个偏小的数当成真值显示，正是这张票要根治的那类毛病。

## 三、由此得到的基线（给「落地：阶段 3」用）

| 这一问 | 结论 |
| --- | --- |
| `counts` 数哪些文件 | 数列举返回的票行（= 文件名以数字加短横开头的 `.md`，且在该工作单元目录下）；地图容器行分开算；`open`/`closed`/`total` 按 `Status:` 行的四词判闭 |
| 工作单元范围 | 按 `repo.effortId`（取值范围就是 `.scratch/<目录名>`）；不给范围时全仓库一起数 |
| `listPage` 怎么翻 | 排序好的票行列表按下标切片，游标 = 下标字符串；数据变了也不会失效 |
| 票文件坏掉报什么 | 内容坏今天不报错（如实记下）；**读不动必须报错**，不许静默少一行 |
| 代价 | 与今天的列举同量级（本来就是全量读），`counts` 不会比 `list` 更贵 |

## 四、本地 Markdown 这一半怎么复现（已在本会话跑过）

做法：在系统临时目录里造工作区（`.scratch/demo/{map.md,issues/}`，按需要放正常票、坏内容票、不按编号命名的文件、多个工作单元、子目录里的票），用仓库里的真实文件系统适配器形状（照 `tests/verify-markdown-backend.js` 第 89-106 行的写法包一份 `path` + `fs`）直接调 `src/host/tracker/backends/markdown/issues.js` 的 `listIssues`，再把结果与「目录里到底有几个票文件」逐张对上。

- 全程只读：工作区建在系统临时目录，没有改仓库里的任何文件，也没有改任何票文件（边界要求的两条都守住了）。
- 「读不动」这一路不是真去改权限，是在测试用的文件系统适配器里，让指定的那个文件名在读的时候抛错——效果与真机上「权限不足 / 读到时已被删 / 被锁住」相同。

这一套可以直接改成阶段 3 的验收脚本（放 `tests/` 下随门禁跑）：断言计数等于目录实情、断言按状态计数、断言按下标切片不重不漏、断言「有文件读不动 ⇒ 报错而不是少一行」。

## 五、GitLab 那一半：静态事实（读代码 + 查官方文档得到，尚未在真机验证）

**读代码得到的两条硬事实**

1. **今天的代码根本不看响应头**。`src/host/tracker/backends/gitlab/client.js:33` 调 `ctx.exec('glab', args, …)`，拿回来的是 `{stdout, stderr, code}` 三样（第 34-35 行），HTTP 响应头不在里面；`issues.js:89` 直接对 `stdout` 做 `JSON.parse`。也就是说「用 `X-Total` 头」这条路要先加一个取头参数的开关（`glab api` 的 `-i`），并且取回来之后不能再直接 `JSON.parse`（响应头在最前面，正文在后面），解析要换新写法。这是落地成本，先记在判据里。
2. **今天 `list` 只取一页**：`issues.js:72` 设了 `per_page=100`，没有 `page`，也没有 `order_by` / `sort`。规格第 12 节把它记成「一页 100 条且不翻页」，属实。

**查官方文档得到的三条**

1. `x-total` 是**偏移分页**（`page` + `per_page`）的响应头之一，另外还有 `x-total-pages` / `x-next-page` / `x-page` / `x-per-page`；**带 keyset 分页的请求不返回这类头**（只有 `Link` 与游标头）。出处：GitLab 官方 REST 文档的 Pagination 与「Pagination response headers」两节（https://docs.gitlab.com/api/rest/ ）。
2. **超过 10000 条的查询，GitLab 不返回 `x-total` 与 `x-total-pages`，也不返回 `rel="last"` 的 Link**（同为上面那节的原文）。反过来看，**返回了 `x-total` 就说明这条查询没被这个上限卡掉**——这一点可以直接当判据用。
3. `GET /projects/:id/issues_statistics` 存在，一次调用回 `{"statistics":{"counts":{"all":…,"closed":…,"opened":…}}}`，正是 `counts` 想要的那三个数；该接口每个项目、子组都可用，要求调用者对该项目有访问权限，且**必须带认证**（未认证会 401）。出处：https://docs.gitlab.com/api/issues_statistics/ 。
4. 列表接口本身（https://docs.gitlab.com/api/issues/ ）：`per_page` 最大 100；`state` 取值是 `opened` / `closed` / `all`；`order_by` 默认 `created_at`、`sort` 默认 `desc`（恰好与规格第 4.2 节要求的「创建时间倒序」一致，但**要显式写出来**，别靠默认值）；`with_labels_details=true` 已经在用，无需改。

**四条只能在真机上回答的问题**（下文第六节给命令）

1. 自建实例上，`x-total` 到底回不回、回的数对不对（尤其要对着网页或 `glab` 自己数出来的数目核）。
2. `issues_statistics` 到底回不回、回的数与「一页一页翻到底数出来的」是否一致（含 `state=opened` 与 `state=closed` 两次分状态核对）。
3. 翻页：`per_page` + `page` 能不能连着翻到底、有没有重复或漏行；`state=opened` / `state=closed` 两侧数出来的总数相加是否等于不筛状态的总数。
4. 杂项但要紧的：`glab api` 的 `-i`（要头）与 `--paginate`（自动翻页）能不能一起用、一起用时输出长什么样；合并请求会不会混进 issues 列表里（GitLab 里合并请求是独立资源，理论上不会，但要用真数据确认一次）。

## 六、GitLab 逐条命令（谁有环境谁照抄）

先备一个工作目录（在你自己那份 GitLab 仓库的克隆里跑，`glab` 会自动认出仓库）：

```bash
# 0. 认出仓库与项目编号（后面所有命令都用它）
glab auth status
RID=$(glab api projects/:fullpath --jq .id)
echo "project id = $RID"
```

**判据 A · 计数口一：`x-total` 响应头**

```bash
# A1 只取一页一条，把响应头也带出来
glab api "projects/$RID/issues?per_page=1&state=all" -i | head -20
# A2 换几种每页条数，看总数一不一样（都应与网页上的数目一致）
for P in 1 20 100; do
  glab api "projects/$RID/issues?per_page=$P&state=all" -i | sed -n 's/^[Xx]-[Tt]otal: *//p' | head -1 | xargs echo "per_page=$P x-total="
done
# A3 分状态各取一次，两数相加应等于 A2 的总数
for S in opened closed; do
  glab api "projects/$RID/issues?per_page=1&state=$S" -i | sed -n 's/^[Xx]-[Tt]otal: *//p' | head -1 | xargs echo "state=$S x-total="
done
# A4 与「一页一页翻到底」数出来的数目核对（不用 -i，纯数条数）
glab api "projects/$RID/issues?per_page=100&state=all" --paginate | jq 'length'
```

A4 若要对照真正翻页（不走 `--paginate` 的自动化），就把 `page=1` / `page=2` / … 逐页取，直到回来空数组为止，把每页条数加总。

**判据 B · 计数口二：`issues_statistics`**

```bash
# B1 不带状态筛选，一次拿三个数
glab api "projects/$RID/issues_statistics" --jq .statistics.counts
# B2 分状态各要一次（文档说支持 state 参数）
glab api "projects/$RID/issues_statistics?state=opened" --jq .statistics.counts
glab api "projects/$RID/issues_statistics?state=closed" --jq .statistics.counts
# B3 带上标签筛选再要一次（规格要求 filter 支持 labels）
glab api "projects/$RID/issues_statistics?labels=<你的一个真标签>" --jq .statistics.counts
```

**判据 C · 翻页稳定性**

```bash
# C1 显式定住排序键，连翻两页，看有没有重复编号
glab api "projects/$RID/issues?per_page=100&page=1&state=all&order_by=created_at&sort=desc" | jq -r '.[].iid'
glab api "projects/$RID/issues?per_page=100&page=2&state=all&order_by=created_at&sort=desc" | jq -r '.[].iid'
# C2 分状态两侧的总数相加 = 不筛状态的总数
for S in opened closed; do
  glab api "projects/$RID/issues?per_page=100&state=$S" --paginate | jq 'length' | xargs echo "state=$S 翻到底共"
done
glab api "projects/$RID/issues?per_page=100&state=all" --paginate | jq 'length' | xargs echo "state=all 翻到底共"
# C3 顺手确认：上面 jq 'length' 拿到的条数里有没有合并请求混进来（理论上不该有）
glab api "projects/$RID/issues?per_page=100&state=all" --paginate | jq '[.[] | select(.merge_requests_count != null or .mr_url != null)] | length'
```

**判据 D · 取头与自动翻页能不能一起用**

```bash
# D1 要头 + 自动翻页
glab api "projects/$RID/issues?per_page=100&state=all" --paginate -i | head -5
# D2 只看这一条能不能成功、输出是「头 + 多段 JSON 数组」还是别的形状
glab api "projects/$RID/issues?per_page=1&state=all" -i | wc -l
```

**判据 E · 边界（各测一次，判定要用「大于等于上限」而不是「大于」）**

```bash
# E1 正好是 100 的整数倍时，最后一页是空数组还是少一条（用 per_page=1 最容易造这种边界）
glab api "projects/$RID/issues?per_page=1&page=<最后一页>"
# E2 空仓库（一张票都没有的测试项目）：statistics 应回 0、x-total 应回 0 或干脆不出现
```

**边界与纪律（跑的时候守住）**

- 只读：本节所有命令都是 GET，不改票、不改标签、不建合并请求。
- 命令里出现的 `<你的一个真标签>` 换成你这个项目里真实存在的标签名，别用一个不存在的标签（那样只能测到「不存在标签回 0」这一种情形）。
- 每条命令都把**原始输出**贴进本票评论（或贴关键若干行），并写清 GitLab 版本（`glab api version` 回的那份里的 `version` 字段）与是不是自建实例——`x-total` 与「超过 10000 条不返回」这两条都可能随版本与实例配置变化。
- 数出来的数目要**与网页上项目议题列表右上角显示的数目核一次**，两边一致才算判据 A/B 通过。

## 七、要维护者拍板的一件事（与 GitLab 无关，本地 Markdown 落地时会遇到）

**`listPage` 的排序键用哪个？** 规格第 4.2 节定的是「创建时间倒序」，理由是翻页期间有人改票也不会让票在页间挪位。但本地 Markdown 今天的列举是按「工作单元 + 编号」排序的（`issues-read.js:118`）。

- **选一**：`listPage` 内部按创建时间倒序重排（只影响按页取的那条路，现有列表顺序一个像素都不动）。
- **选二**：把列举也一并改成创建时间倒序（列表与翻页同序，但会改动现有列表的行序，超出本票范围）。

我倾向**选一**：规格要的是「翻页稳定」，而创建时间在本地 Markdown 里来自文件修改时间（`issues-locate.js:135-142`），本来就不是票的固有属性；只有按页取的那条路需要它，没必要连带改列表。

## 八、进度

本地 Markdown 三问已实测出结论（第二节，含第四节可复现的做法），GitLab 那一半的判据与命令已写好（第五、六节）。**进度挂在 60%**：本票的交付物是「判据 + 命令 + 实测结果」，现在还缺 GitLab 的实测结果，所以不关票。

下一步：谁有 GitLab 环境谁照第六节跑一遍，把原始输出与结论贴回本票；两组结果齐了即可 100% 关闭，「落地：阶段 3」随后按第二节与第五节的结论动手。

