const fs=require("fs");const path=require("path");let failed=false,total=0,passed=0;function check(ok,msg){total++;if(ok)passed++;else failed=true;console.log((ok?"  PASS ":"  FAIL ")+msg);}console.log("== #190 → #231 契约动作形态 ==");function read(p){return fs.readFileSync(path.resolve(p),"utf8");}console.log("\n-- Dock.js --");{const src=read("src/client/panel/Dock.js");check(/repositoryActionOf/.test(src)&&/wf\.openFolder/.test(src),"Dock markdown→openRepository 动作");check(/host\.call.*wf\.openFolder.*cwd/.test(src),"Dock host.call");check(/target:\s*'_blank'/.test(src),"Dock url 型原生新窗");const a=src.indexOf("repositoryActionOf"),b=src.indexOf("if (!href)");check(a>0&&b>0&&a<b,"Dock order（动作判定先于无链接回退）");check(/act === 'folder'[\s\S]*?h\('a'/.test(src),"Dock folder 型可点芯片");check(/href:\s*href/.test(src),"Dock href");}console.log("\n-- Overlay.js --");{const src=read("src/client/panel/Overlay.js");check(/repositoryActionOf/.test(src)&&/wf\.openFolder/.test(src),"Overlay markdown→openRepository 动作");check(/host\.call.*wf\.openFolder/.test(src),"Overlay host.call");check(/openUrl|_blank/.test(src),"Overlay openUrl 形态保留");}console.log("\n-- Host --");{const src=read("src/host/index.js");check(/wf\.openFolder/.test(src),"host handle");check(/platform\.resolveExecutable/.test(src),"platform");check(/explorer/.test(src),"explorer");check(/xdg-open/.test(src),"xdg-open");check(/subprocess\.spawn/.test(src),"spawn");check(/platform\.path/.test(src),"path");}console.log(`\n${failed?"FAIL":"PASS"} ${passed}/${total}`);process.exit(failed?1:0);
